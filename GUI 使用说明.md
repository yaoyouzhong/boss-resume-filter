# BOSS 简历筛选器 - 图形界面版使用说明

## 启动方式

### 方式 1：双击启动
双击 `gui.bat` 文件即可启动图形界面。

### 方式 2：命令行启动
```bash
python gui_main.py
```

## 界面功能

左侧导航栏共 6 个页面（首页、岗位配置、运行控制、筛选结果、学历核验、数据统计），底部另有系统设置入口。

### 1. 首页

启动后默认显示招聘工作台。首页只汇总有助于立即判断和操作的信息，不替代筛选结果页、今日待办或系统设置中的完整功能。

- **岗位范围**：选择单个岗位或“全部岗位”，今日任务和候选人摘要会使用同一范围重新汇总；岗位名称较长时可在加宽的下拉框中查看。
- **最近扫描**：显示最近一次已经结束的扫描时间、岗位范围和完成状态；没有记录时不会用当前时间伪造扫描时间。
- **今日行动**：按“待核实 → 待复核 → 待联系”的优先级汇总，每位候选人只进入当前最优先的一项，避免重复计数。点击任一非零任务，会打开同一个“今日待办”工作台并自动应用对应筛选；这不是另一套独立候选人工作台。
- **运行准备**：显示 API Key 是否已配置、Chrome 是否已连接，以及候选人数据和联系清单能否正常读取。状态只描述本机已经验证的信息，不会把“已配置”误写成网络一定可用；异常项会提供对应处理入口。
- **候选人摘要**：显示通过筛选、强烈推荐、推荐和已打招呼人数。点击数字可打开该范围的候选人明细，点击“查看全部结果”进入筛选结果页。
- **常用工作**：学历核验排在导入外部候选人之前；数据备份与恢复和系统设置作为维护入口放在右侧。首页进入系统设置时会回到页面顶部。

### 2. 岗位配置

**选择岗位**：从下拉列表选择已配置的岗位；岗位名称右侧会显示“已保存”或“有未保存修改”

**新建岗位**：建立一份新岗位草稿，并展开“招聘需求”区域

**更多操作**：集中提供导入配置、导出配置和删除当前岗位等低频操作

**岗位配置表单**：
- 已有岗位默认收起“招聘需求”，直接展示基础筛选条件；点击标题栏或右侧箭头即可展开，折叠时会显示招聘需求的保存状态
- **岗位名称**：岗位的名称（如：高级 Java 工程师）
- **最低学历**：要求的最低学历（不限/高中/中专/大专/本科/硕士/博士）
- **最低经验**：要求的最少工作年限
- **最大年龄**：候选人年龄上限，留空不限制
- **薪资范围**：岗位薪资预算范围（如 12K ~ 15K），留空不限制
- **工作地点**：自由输入，支持多地点（如：南京、上海/杭州）
- **技能评分条件**：维护参与评分的技能关键词；权重范围为 1-3，可点击输入框右侧箭头或使用鼠标滚轮逐级调整；同一技能的别名或格式差异会自动合并，普通技能与优先项重复时保留优先项
- **必要条件**：只保存会直接淘汰候选人的明确硬约束。选择条件类型（简单匹配 / OR满足任一 / AND全部满足），输入逗号分隔的关键词（支持全角/半角逗号）

**招聘需求解析**：

- 在输入框中粘贴招聘需求，点击"解析招聘需求"自动提取：岗位名称、经验、学历、年龄上限、薪资范围、工作地点、技能关键词、优先项和必要条件
- 解析流程先用本地规则生成基础结果；如果已配置可用的大模型和 API Key，会自动启用 AI 增强解析，补充和修正基本信息、技能关键词和优先项。AI 提出的必要条件只有在原文“必要条件/硬性约束”段或明确强约束语句中找到证据后才会补充，且不会删除本地规则或用户已确认的条件
- "全栈开发经验优先"、"数据库运维经验优先"、"证券行业优先"等优先项会进入 `preferred_keywords`，作为排序加分项，不会稀释普通技能关键词匹配分
- "金融投资行业经验，债券、基金、期货、期权等"这类必要条件会尽量解析为 OR 条件，候选人满足任一方向即可通过
- 点击"招聘需求示例"按钮可一键填充模板文本，方便按格式修改（仅在"新建岗位"模式下可用）
- 新建岗位时按“填入需求→解析需求→检查结果→保存配置”四步完成，不再使用闪烁提示干扰编辑

**配置质量**：保存区持续显示质量分、阻断项、提醒项和建议项；点击右侧“查看详情”可打开具体检查结果。存在未保存修改时可恢复已保存内容，新岗位草稿可直接清空。

切换岗位、新建岗位、导入配置或退出程序前，如果当前内容尚未保存，系统会询问保存、放弃或取消操作，避免误丢配置。

### 3. 系统设置：模型配置

配置 AI 辅助评估使用的大模型。

**服务商选择**：支持 qwen、deepseek、kimi、zhipu、minimax、xiaomi、stepfun、openai、anthropic、custom 等 10 种服务商

**服务类型识别**：系统结合服务商和 Base URL 判断官方 API、官方套餐入口或中转服务；Token Plan、Coding Plan、Step Plan 等官方套餐仍显示为官方服务，服务商与地址不匹配或使用未知域名时才显示为中转/自定义服务

**API Key**：
- 长按眼睛图标可临时显示明文，松开后自动隐藏
- Key 加密存储在系统钥匙串中，配置文件不含明文
- 相同服务商和 Base URL 的模型共用同一个 Key

**模型选择**：
- 输入 API Key 后可点击模型列表按钮动态获取可用模型
- 也可手动输入模型名称

**保存与使用**：
- 点击“保存模型”将当前服务商、Base URL 和模型加入“已保存模型”
- 在“使用中的模型”分别选择默认 AI 模型和学历核验模型；学历核验模型可选择“跟随默认 AI 模型”
- 点击两个模型右侧的信号灯测试各自的实际连接；成功显示绿灯，失败显示红灯，未测试为黄灯
- 已保存模型列表支持批量测试连通性和删除；正在使用的模型需要先在“使用中的模型”中切换后才能删除

**数据备份与恢复**：
- “导出数据备份”会统一备份候选人、岗位配置、联系清单和程序管理的简历副本，并在导出后校验完整性
- “从备份恢复”会先检查 ZIP 完整性和数据格式，确认后再事务式替换关联数据；恢复前自动保留当前数据恢复点，恢复后清理无人引用的旧简历副本，中断后下次启动会继续完成或回退
- “简历存储体检”会核对缺失引用、受管目录外引用、残留评估状态和孤立文件；体检本身只读，确认“修复并清理”后才会清除失效状态并删除无人引用的受管副本
- 重新导入、撤销简历评估、删除或清空候选人时，只回收已经没有其他候选人记录引用的受管副本；共享副本和目录外文件不会删除
- 扫描或联系任务进行中不能备份或恢复，避免备份到一半状态
- 导出的 ZIP 当前不加密，包含候选人资料和简历时必须保存在受控位置

**故障诊断**：
- “导出脱敏诊断包”收集当前版本、系统与界面环境、数据结构计数，以及最近的运行和应用日志
- 诊断包不包含候选人原始数据、简历、岗位内容、API Key、Cookie 或 Chrome 资料
- 日志会隐藏候选人身份、岗位名称、手机号、邮箱、身份证号、请求凭据和本机用户路径；发现原始值残留时会拒绝导出
- 诊断包只保留有限数量和大小的最近日志，并附带文件完整性清单；发送给他人前仍建议打开 ZIP 人工查看

### 4. 运行

**浏览器状态**：显示 Chrome 浏览器连接状态（未连接/已连接），点击"检测/连接浏览器"按钮进行连接。连接到 BOSS 直聘推荐牛人页面后，程序会自动检查页面结构；账号没有已发布职位时会明确提示暂时不能扫描，不会误报为页面选择器异常

**选择岗位**：默认使用最近一次运行时选择的岗位；新建并保存岗位后，会自动作为运行页默认岗位。“全部岗位”仍可手动选择，用于依次处理所有岗位

**候选人提取**：程序先补全页面已有的信息，再通过页面滚动确认可见候选人；必要时补全已在页面出现的候选人详情。候选人集合始终以页面实际出现的人为准，不会把额外读取到的人直接加入结果。页面滚动采用随机等待，默认每次滚动后约 1.5-4 秒再继续；每滚动 5-10 轮会额外暂停约 8-15 秒

**AI 辅助评估**：勾选后对通过筛选的候选人调用 LLM 做二次评分（±15 分调整），需先在「系统设置」中配置好默认 AI 模型

**高级运行设置**：默认折叠，集中管理以下四项；调高参数可能增加扫描耗时、页面访问量或联系准备风险，超过建议值时开始前会列出具体项目并再次确认
- **滚动轮次**：扫描时自动滚动的次数，默认 50，推荐不超过 100；支持鼠标滚轮微调
- **AI 响应超时**：仅在启用 AI 辅助评估时可设置，控制单次 AI 响应的最长等待时间
- **扫描增强**：开启“自动补全候选人详情”后，程序会在页面扫描基础上补全经验、薪资、城市等信息；“最多读取”控制最多补全多少页
- **后续联系**：开启“扫描后准备联系信息”后，可提升后续联系成功率；“最多准备”控制本轮最多准备多少人

**运行日志**：界面日志只显示扫描过程及必须立即关注的访问保护信息；`logs/run-YYYYMMDD.log` 保留本轮参数以及扫描、AI 评估、联系准备和发送全过程，`logs/app-YYYYMMDD.log` 保留浏览器连接、后台检查、界面和联系清单恢复等应用诊断。前台联系过程会同时写入两个文件，但不会混入扫描界面。两类日志都会隐藏请求凭据，并自动清理 30 天前的日志

**扫描后的联系安排**：
- **仅保存筛选结果**：只筛选和保存候选人，不加入联系清单（默认）
- **将强烈推荐加入联系清单**：扫描正常完成后，将 75 分及以上且符合联系条件的候选人加入清单
- **将推荐及以上加入联系清单**：扫描正常完成后，将 65 分及以上且符合联系条件的候选人加入清单

运行控制只负责扫描、筛选和生成联系清单，不会直接发送消息。请在筛选结果页进入“联系候选人”工作台，核对人数和页面要求后开始发送。

**筛选进度**：实时显示当前扫描/筛选进度百分比

**控制按钮**：
- **开始**：启动运行
- **停止**：立即停止运行（停止信号穿透所有循环，自动保存进度）

**运行前与结果摘要**：开始后会先检查岗位配置、BOSS 当前岗位和页面状态。BOSS 岗位名称与本地配置不一致时会列出双方名称，确认对应关系无误后仍可继续；配置存在风险时也会要求确认。账号无已发布职位时直接说明原因。只有正常完成的扫描会按所选策略生成联系清单，停止或异常时仅保存已取得的候选人结果。运行结束、停止或异常后，“本轮结果摘要”按筛选结果、扫描范围和筛选明细分段显示，最多展示 10 行，超出后在摘要内滚动；运行日志只保留过程明细和一行终态。“达到轮次上限”属于本轮正常结束，但表示尚未检测到列表底部，也未连续 5 轮无新增候选人，因此摘要会用黄色提示“未确认扫描到底”；适当提高“滚动轮次”后重新运行即可继续确认。

**访问保护**：遇到登录失效、安全验证、操作频繁或平台限制响应时，程序会停止本轮后续页面读取和联系操作并进入冷却；即使已手动完成验证，本轮也不会自动继续。已经取得的候选人仍会完成规则筛选、AI 评估和保存，相关状态同时记录在界面日志和当日运行日志中。

**运行日志**：实时显示程序运行过程中的日志信息，支持清空日志

### 5. 筛选结果

**统计信息**：总人数、通过人数、已打招呼人数

**候选人列表**：默认显示“全部记录”，避免因结果范围过滤而误以为候选人数据丢失；已屏蔽的候选人默认不显示
- 字段依次为姓名、性别、工作年限、薪资、技能匹配、匹配分、AI 评估、推荐指数、状态、年龄、学历、求职状态、毕业学校和最近公司；普通窗口可使用表格底部横向滚动条查看后面的字段
- 姓名、性别、匹配分、推荐指数和状态均可搜索；毕业学校、最近公司或求职状态显示不全时，鼠标悬停可查看完整内容
- 待复核包含尚未完成人工判断的 55-64 分候选人，以及学历、工作经验等硬条件证据仍待确认的候选人；状态列保留简短“待复核”标记，鼠标悬停时显示具体复核原因
- “复核通过”范围包含所有经人工确认通过的候选人，包括普通待定候选人和学历等硬条件核实通过的候选人；原匹配分和推荐指数保持不变。已经标记“合适”的普通待定候选人同样视为完成人工判断。低于 55 分本身属于淘汰；硬条件复核通过后会保留“复核通过”记录，但不会再要求做第二次评分复核，也不会因此取得联系资格
- 待复核可以明确选择“确认通过”或“确认不通过”；不通过后结束待复核并保留在淘汰记录。误推、放弃、不合适、已归档或屏蔽也会结束尚未完成的复核，不再反复出现在待复核任务中
- AI 评估调用失败不作为待复核或联系阻断条件；规则分和资格已通过时仍可进入推荐和联系清单，AI 评估列显示“失败”，候选人详情保留具体原因
- 发送待核实属于沟通状态，不会把原本符合要求的候选人移入待复核，但会阻止再次加入联系清单；即使随后被放弃或屏蔽，也必须先核实实际是否已经发送，避免重复联系
- 列表显示当前人数和筛选后总人数，不限制为前 100 人
- “结果范围”可切换推荐候选人、复核通过、待复核、淘汰记录、外部导入和全部记录；「外部导入」按来源展示全部外渠道导入的候选人（含未通过筛选的记录），适合导入后集中核对。推荐范围与复核通过范围允许重叠，例如高分候选人的学历核实通过后会同时符合两者。该选项只过滤下方表格，顶部统计保持当前岗位和日期的统一口径；普通首次淘汰只进入运行摘要，不长期保存
- 时间范围默认“全部时间”，可切换今天、近 7 天、近 30 天或自定义日期；选择自定义后才显示起止日期。日期过滤按候选人首次发现时间计算，重复扫描只更新最近评估时间
- 结果表有候选人时“查看与复核”即可使用；未选择具体行时会自动从当前结果第一位开始，也可双击候选人或使用右键菜单进入同一工作台。工作台首先给出下一步、判断依据、关键匹配和风险，再通过“完整资料”查看全部信息，并支持上一位、下一位连续处理

**操作按钮**：
- **刷新结果**：从 candidates_all.json 加载最新数据
- **今日待办**：先按立即处理、已逾期、今天、待安排和以后区分时间，再按复核、联系、跟进等业务动作归组；发送待核实优先显示，缺失分数或未知状态进入“状态异常待处理”；未来任务不计入今日人数，缺少日期的旧记录进入待安排，已约面不会因漏填日期而消失。每人只进入一个最高优先级任务；“待复核”与结果范围使用同一判断口径，并可继续展开学历形式、求职状态、评分等主要原因
- **查看与复核**：处理单个候选人的主入口；未选择候选人时从当前结果第一位开始，首次发现时间以“年-月-日 时:分”显示；底部固定分为上下两层，待复核时第一层显示“复核结论”，将“确认通过”和“确认不通过”并排提供二选一，其他状态第一层显示“建议下一步”，第二层统一显示“其他操作”。所有按钮使用一致的字体和尺寸；确认通过或不通过后停留在当前候选人，只有用户主动点击上一位或下一位才切换
- **联系候选人**：打开联系工作台，查看待发送、待核实和发送失败的候选人；支持单选、多选、Ctrl+A 全选当前列表以及右键联系，清单会在程序重启后恢复。发送依赖当前岗位页面时，如果 BOSS 岗位名称与本地配置不同，会在列出双方名称后等待确认，确认对应关系无误即可继续
- **更多操作**：集中放置导入外部候选人、候选人状态体检、导出 Excel 和清空候选人等辅助功能；导出 Excel 使用当前结果表的岗位、时间范围、结果范围和黑名单显示条件，并保留表格中可见的低分待复核人员；清空前会自动备份，并可保留已沟通记录，已屏蔽候选人始终保留为黑名单

**导入外部候选人**：针对猎头、内推、智联、猎聘等 BOSS 之外渠道获得的简历。在对话框中选择简历文件（PDF/Word/TXT/MD/RTF/HTML，浏览时可多选；旧版 .doc 会自动识别实际格式解析，必要时由本机安装的 Word 辅助提取全文，招聘网站导出的 .doc 无需 Word 即可导入，失败时会提示手动另存为 .docx）、确认姓名（按文件名预填，可修改）、选择归属岗位并填写来源渠道后，系统解析简历全文并立即完成筛选评分，候选人随即出现在筛选结果表中；已配置 AI 模型时继续在后台完成简历评估（状态列显示"AI评估中..."，期间可正常操作界面），完成后弹出结果摘要（筛选评分、评估调整、最终分和评估理由）。**一次选择多份简历即进入批量导入**：整批统一归属岗位、来源渠道和备注，姓名逐份按文件名自动提取（无法识别时以文件名代替并在汇总标注"待核对"）；导入过程多份简历并行解析（旧版 .doc 转换较慢，并行可明显缩短整批时间）并显示进度条，可随时取消（已完成的导入保留，进行中的解析会自行结束）；结束后对话框给出逐人汇总（入库总数及其中通过筛选/未通过人数；有重复跳过或失败时才列出对应人数及原因；明细 5 条以内全部展示，更多时可上下滚动；未通过筛选的记录保留在"淘汰记录"中，通过筛选但低于 55 分的人也会单独说明），同岗位同名的已有外部候选人会自动跳过并列示。批量导入同样自动完成筛选评分和简历 AI 评估（仅对达到 55 分通过线的人自动评估，低分导入者可右键手动补评）。导入时会从简历自动识别薪资（"元/月"和"年薪/年收入"口径都会折算为月薪，如年薪 14 万记为 11.7K）、年龄（可由出生日期换算）、学历（专科按大专处理）、毕业学校（取最高学历所在的学校）和最近公司（取时间上最近的一段工作经历），识别不到的字段留空，不影响筛选评分。对话框底部的「AI 增强识别」开关默认关闭；未配置默认 AI 模型或 API Key 时开关不可开启。开启后，规则未识别到的缺失字段（如格式不常见的薪资、年龄、期望城市）由 AI 补全并参与筛选，候选人详情的「AI 画像增强」段会列出补全内容；AI 识别结果与规则结果不一致时保留规则值，并将该候选人转入「待复核」由人工确认；学历、学校、公司仅作展示不参与筛选；AI 调用失败不影响导入，只记录原因。导入的候选人与扫描候选人一样进入筛选结果、统计看板和 Excel 导出（导出有"来源渠道"列），可使用复核、反馈、跟进、黑名单和简历评估等全部管理功能；未通过筛选的会保留在"淘汰记录"中，淘汰行的匹配分列展示参考匹配分（剔除学历等硬条件后按技能和经验估算的分数），供核对是否存在误杀，该分数不改变淘汰结论、不计入统计。外部候选人无法在 BOSS 上联系，因此不会进入联系清单和"今日待办"的待打招呼，请通过来源渠道自行联系后更新跟进状态。单份导入时若同岗位同名已存在外部候选人，会先提示确认

**编辑候选人信息**：简历解析不完善时，可在外部候选人的右键菜单中选择"编辑候选人信息…"（仅外部候选人显示）手工修正。对话框预填当前识别结果，可修改姓名、性别、年龄、学历、工作年限、期望薪资、期望城市、求职状态、毕业学校和最近公司，也可在下拉中调整归属岗位；留空表示该字段未识别。只改姓名、学校或公司时直接保存；修改性别、年龄、学历、工作年限、薪资、城市或求职状态后，系统会以修正值为准重新筛选评分（修正的学历同样参与学历要求判断），已有的简历评估结果继续有效；调整归属岗位时按新岗位规则重新评分，原岗位的 AI 评估和复核结论清除，并在配置 AI 模型时自动重新评估，反馈、跟进、备注和黑名单始终保留。目标岗位下已存在同名外部候选人时会阻止保存。BOSS 扫描候选人的岗位关联线上沟通上下文，不支持编辑

### 6. 候选人明细与复核工作台

点击筛选结果页的统计指标数字（强烈推荐、推荐、待定、已打招呼），弹出候选人明细窗口。

**弹窗内容**：
- 标题显示分类名称（强烈推荐 / 推荐 / 待定 / 已打招呼）
- 统计行显示总人数和已打招呼人数
- 表格列：姓名、工作年限、薪资、匹配分、推荐指数（强烈推荐/推荐/待定）、状态（已招呼/未招呼）、技能匹配

**窗口关系**：统计明细、今日待办、状态体检、联系候选人、查看与复核、更新日志和关于等信息或工作台窗口不锁定主窗口；确认发送、更新跟进、标记反馈、填写屏蔽原因等必须完成或取消的操作窗口会暂时锁定其父窗口

**右键菜单**（在表格行上右键）：

**单选模式**（右键单个候选人）：
- **查看与复核**：打开候选人查看与复核工作台。首屏分开显示筛选结论、复核状态和沟通状态，并给出下一步、评分拆解、命中证据和风险；“完整资料”保留核心信息、教育信息、AI 评估、跟进、反馈、黑名单和候选人摘要等全部内容。工作台可直接确认通过、确认不通过、更新跟进、标记反馈、导入简历或发起 AI 评估
- **导入简历**：选择候选人的 PDF/Word 简历后，基于完整简历做二次评估；结果会写入候选人数据、详情页和 Excel 导出
- **撤销简历评估**：已导入简历并完成二次评估的候选人可撤销评估，清空简历文件和二次评估结果，分数回退到一次评估状态
- **加入联系清单**：仅在候选人已完成复核且符合联系条件时显示；与复核工作台使用同一校验逻辑，加入后统一在联系工作台中确认和发送
- **核实发送结果**：发送结果无法自动确认时显示；进入联系工作台后，根据 BOSS 沟通列表明确选择“确认已发送”或“确认未发送”
- **更新跟进**：更新候选人的跟进状态（未沟通/已打招呼/已回复/待约面/已约面/不合适/已归档）、下次跟进日期和备注；支持今天、明天、3 天后、7 天后及不设置。打招呼成功、待约面和已约面默认安排次日处理，待约面和已约面保存时必须填写日期，结束状态自动清除提醒；把已沟通候选人改回“未沟通”会再次确认，并清除错误的已打招呼时间、发送方式、待核实状态和跟进日期。标记不合适且尚无反馈时，会继续打开反馈表单并默认选择“放弃”，避免误记为规则误推。右键菜单还可快捷标记已回复、推进到待约面或明天再跟进。跟进状态和日期会写入候选人数据、详情页和 Excel
- **标记反馈**：标记候选人的人工反馈状态（合适/误推/误杀/放弃）和备注；误推或误杀至少选择一个结构化原因。反馈会写入候选人数据、详情页和 Excel；岗位复盘会纳入低分误杀，反馈不足 5 条时只提示继续积累样本
- **加入黑名单**：记录屏蔽原因并按 `geek_id` 跨岗位屏蔽该候选人；后续扫描、统计和默认 Excel 导出都会跳过，只有在结果页主动显示黑名单并导出当前表格时才会保留并标明屏蔽状态
- **移除此人**：从列表和 `candidates_all.json` 中删除该候选人，弹窗保持打开并即时更新统计数字

**多选模式**（Ctrl/Shift 多选后右键）：
- **加入联系清单**：把选中的候选人加入清单，系统会先跳过已沟通、硬性条件待确认、低于 55 分、人工反馈为误推/放弃或状态不适合发送的候选人；55-64 分的普通待定候选人需先完成人工复核。仅 AI 评估调用失败、但规则分和资格已通过的候选人无需额外确认，可以进入手动或自动联系清单。已具备发送条件的候选人可直接发送，其他候选人需要浏览器在对应岗位的推荐牛人页面

**联系工作台的安全规则**：未选择候选人时，顶部按钮处理全部待发送候选人；选择一人或多人后只处理选中项，右键菜单提供相同的联系入口。开始发送前会展示已就绪人数和需要岗位推荐页的人数并再次确认。每位候选人发送前都会重新读取最新状态；已沟通、已屏蔽、硬性条件待确认或已不符合联系条件时自动跳过，已持久化的普通待定人工批准会继续有效。复核、反馈、跟进或屏蔽状态改变后，活动清单行会立即更新，不能继续执行已经失效的“加入”或“发送”动作。需要推荐页的候选人只会在对应岗位页面发送，切错岗位时保留为待发送。程序在发送过程中退出时，候选人记录和联系清单都会恢复为“待核实”，不会自动重发；待核实记录不能直接移除，必须明确确认已发送或未发送。
- **批量AI评估（X人）**：X 为本次实际可评估人数；已评估和正在评估的人员会明确跳过，所选人员均不需要新评估时不显示该操作；跨岗位选择时按岗位分别使用对应岗位要求，缺少岗位配置的候选人不执行评估
- **移除选中**：批量删除选中的候选人
- **导出选中**：将选中的多名候选人导出为独立 Excel 文件

**首页候选人摘要**也支持同样的明细弹窗和右键菜单（点击“通过筛选”“强烈推荐”等数字）。

### 7. 学历核验

- 支持一次导入多张 JPG、JPEG、PNG、BMP、WEBP 图片或 PDF 文件的毕业证书
- 待核验队列独立显示每张证书的姓名、证书编号、学校、专业、核验状态和截图状态，点击队列可查看当前证书
- 页面按“识别证书 → 打开学信网验证 → 一键批量截图”展示操作顺序，三个步骤共用进度提示；工具栏按钮默认处理全部已导入证书
- 队列支持 Ctrl/Shift 多选后批量移除；右键菜单中的识别、学信网验证和删除只处理右键点击的单条记录
- 批量识别最多 3 路并发，队列和进度区会逐条显示识别状态；导入多张证书后不会自动聚焦第一张
- 图片预览先按 EXIF 中明确的方向信息纠正；没有可靠 EXIF 信息时保持原样
- 图片模型通过四方向对照图判断是否还需旋转 90°、180°或270°；方向置信度不足时保持当前方向
- 预览卡片标题栏右侧保留”顺转 90°”作为人工兜底；用户点击后锁定人工方向，重新识别不会覆盖
- 双击证书预览可打开原图查看；PDF 文档没有图片预览
- PDF 文件通过提取文本层识别（走文本模型，不渲染图片），预览区只显示文字提示；扫描件 PDF 无文本层时会提示”请导出为图片后导入”，改用图片格式即可正常识别
- 右侧识别结果仍只显示姓名和证书编号，学校、专业只在队列中展示
- 使用当前配置的大模型识别姓名和证书编号（图片走视觉模型，PDF 走文本模型）
- 识别结果必须由 HR 人工确认或修正后，再打开学信网填写；验证时提交的是界面中最后保存的修正值
- 学信网验证会为每条有效记录创建独立标签页，自动填写姓名和证书编号、识别图片验证码并提交；字符验证码和算术验证码均支持自动识别，连续失败后可点击“重试异常验证码”再次处理
- 页面会同步显示等待扫码、二维码过期、查询无记录、结果已出现、验证码失败或页面打开失败等实际状态；二维码过期时优先自动刷新，无需重新提交查询
- 手机扫码并确认后，结果页应保持打开，随后点击“一键批量截图”；只有存在核验结果的记录才会启动浏览器检查，查询无记录的项目会标记为“无需截图”
- 第一次截图时选择保存位置，后续会自动沿用；保存位置未设置或目录已经失效时才会再次询问
- 截图只保留学信网页面内容，去除浏览器外框和周边大片空白，并统一为固定宽度 PNG；重复执行会自动跳过已有有效截图，只补充遗漏项目
- 识别时证书图片、PDF 内容和学信网图片验证码会发送给当前配置的 AI 服务，请在取得候选人授权后使用

### 8. 数据统计

侧边栏第 6 个导航项，按岗位维度聚合历史筛选和打招呼数据。

**过滤条件**：
- **岗位过滤**：选择单个岗位或"全部岗位"
- **时间范围**：今天 / 本周 / 全部

**汇总卡片**：
- 通过筛选人数、强烈推荐人数、推荐人数、已打招呼人数

**岗位明细表格**（9 列精简布局）：
- 岗位名称、筛选分布（总数+强推/推荐/待定）、已打招呼(率)、已反馈、合适率、误推率、已回复(率)、已约面(率)、平均分
- 低于 55 分的候选人不计入统计
- 合适率/误推率只按有效人工反馈计算（合适/误推/误杀/放弃）
- 已回复/已约面列内嵌百分比，分别按已打招呼及后续状态、已回复及后续状态计算
- 双击岗位行或右键选择“岗位复盘”，可查看结构化反馈趋势；复盘会保留低分误杀样本。反馈达到 5 条后，建议会显示具体样本证据，并可直接定位到对应岗位的招聘需求、学历、经验、薪资、地点、技能或必要条件区域；定位只负责打开和高亮，不会自动修改或保存岗位配置。反馈不足 5 条时只展示事实，不提供规则调整入口

如果真实反馈暂时不足 5 条，可在项目目录运行 `python tests/manual/preview_job_review_locator.py`。该预览固定使用 5 条内存合成反馈，覆盖招聘需求、学历、经验、薪资、地点、技能和必要条件 7 类定位；它不会启动主程序、读取业务 JSON 或提供保存入口。每次点击定位后，使用主窗口的“打开岗位复盘”继续检查下一项。自动烟测可运行 `python tests/manual/preview_job_review_locator.py --smoke`。

点击"刷新"按钮或切换过滤条件时自动重新统计。


## 使用流程

### 初次使用

1. **配置岗位**：
   - 点击"新建岗位"
   - 输入岗位名称、经验、学历和性别要求
   - 输入技能关键词（逗号分隔）
   - 点击"保存配置"

2. **开始扫描**：
   - 选择要处理的岗位（或"全部岗位"）
   - 设置扫描后的联系安排；需要时展开“高级运行设置”调整滚动轮次等参数
   - 点击"开始"
   - 手动导航到 BOSS 直聘推荐页面
   - 等待程序自动运行

3. **查看结果**：
   - 运行完成后，点击"刷新结果"
   - 查看候选人列表
   - 如需报表，在“更多操作”中点击“导出 Excel”；导出范围与当前表格筛选条件一致

## 注意事项

1. **浏览器要求**：需要安装 Chrome 浏览器（程序可自动启动 Chrome 并导航到推荐页面）
2. **手动导航**：如果浏览器未自动导航到推荐页面，请手动导航到 BOSS 直聘推荐页面
3. **数据备份**：定期备份 candidates_all.json 文件
4. **网络稳定**：保持网络连接稳定
5. **安全验证弹窗**：运行过程中如果 BOSS 直聘弹出滑块/图形验证码，程序会自动暂停并弹出对话框通知。在浏览器中手动完成验证后，点击对话框中的「是」继续等待验证完成；如果是误报（浏览器中没有看到验证码），点击「否」跳过等待继续运行

## 常见问题

**Q: 点击开始没有反应？**
A: 检查是否已安装 Chrome 浏览器，确保没有其他程序占用。

**Q: 日志显示乱码？**
A: 优先使用当前版本运行；如果仍在旧终端看到乱码，一般不影响功能，可在 PowerShell 中切换到 UTF-8 后重试。

**Q: 如何查看详细的候选人评分？**
A: 在“筛选结果”中选择候选人并打开“查看与复核”，首屏可查看评分拆解、匹配证据和风险；“完整资料”中可进一步查看 AI 评估和简历评估理由。评分采用四维模型：基础 25 + 技能 0~50 + 经验超额 0~15 + 学历档次 0~10。
