# 更新日志

## v2.17 — 岗位配置体检、反馈复盘与打招呼队列

### 新增功能

- **岗位配置保存前体检**：保存岗位配置时自动检查明显冲突、重复项和过窄条件，严重问题阻止保存，提醒项可确认后继续保存
- **反馈原因结构化**：标记候选人反馈时可选择技能不匹配、行业经验不符、年限判断偏差、薪资不合适、地点不合适、规则过宽等常用原因，保留备注补充说明，便于后续按岗位复盘
- **岗位级复盘**：数据统计页新增岗位复盘入口，按岗位汇总反馈分布、原因 Top 项、样本概览、已回复、已约面、平均分和调整建议
- **打招呼队列工作台**：筛选结果页新增打招呼队列，多选候选人可先加入队列，再统一查看发送方式、执行状态和失败原因后发送

### 体验优化

- **本机模型配置隔离**：源码运行时的模型选择保存到本机配置文件，避免刷新模型列表后影响发布默认配置
- **打招呼队列加入前过滤**：加入队列时自动跳过已沟通、需人工确认、分数不足或状态不适合自动发送的候选人，减少无效队列项
- **打招呼队列执行校验**：开始发送前区分可直接发送和需要推荐页支持的人数，并检查浏览器未连接、连接断开、未登录或不在 BOSS 页面等情况，避免无效发送
- **打招呼队列状态完善**：队列项记录更新时间和匹配分，对沟通上限、次数耗尽和待确认结果给出更清晰的状态
- **岗位配置体检引导**：体检弹窗提供返回修改入口，发现严重问题时阻止保存，提醒项可确认后继续保存
- **岗位配置输入提示优化**：年龄等字段提示更明确，支持填写整数或留空表示不限制
- **安全验证提示优化**：检测到安全验证弹窗时明确提示在浏览器中手动完成验证，再继续运行
- **自动更新检查降噪**：Gitee 更新源请求超时时会自动重试，启动静默检查不再把可恢复的备用源切换显示成报错
- **本地回归日志显示优化**：单元回归输出中的中文内容可正常显示，失败时会打印更完整的错误信息，便于定位问题
- **工作区清理预览**：新增本地清理预览工具，默认只列出可清理的缓存、日志和临时文件，不会直接删除内容

### 问题修复

- **独立学历核验模型更新**：独立学历证书核验助手默认改用新的 Kimi 接入模型，修复旧模型不可用导致证书识别失败的问题

## v2.16.1 — 模型配置与学历核验修复

### 问题修复

- **多选模型批量保存**：系统设置中选择多个模型后，点击保存可一次性将全部选中模型加入已保存列表，此前仅保存其中一个；保存只追加到列表，不切换当前正在使用的模型，需要切换时双击列表项
- **多选删除批量执行**：系统设置多选模型删除时一次性删除全部选中项，此前只删除其中一个
- **删除模型保留当前配置**：删除已保存模型时不再误清空当前正在使用的模型配置
- **学历核验识别失败状态修正**：模型未能识别出证书内容时状态正确标记为识别失败，此前会误显示为已识别
- **学历核验模型提示一致性**：选择学历核验模型时，视觉模型推荐列表与实际判定保持一致，推荐智谱 GLM-5V、Kimi K2.5 等真正支持图片识别的模型，避免误配文本模型后无法识别

## v2.16 — AI 评估体系升级

### 新增功能

- **AI 硬条件复核扩展**：AI 评估同步复核学历、经验、年龄、薪资、工作地点、求职状态 6 个维度；复核结论和原文证据在候选人详情弹窗展示，方便核查 AI 判断依据
- **AI 多维度评分**：新增技能深度、经验质量、行业匹配、发展潜力 4 个评分维度，详情弹窗可查看各维度得分
- **AI 评估调整幅度扩大**：AI 对规则评分的调整范围从 ±10 扩大至 ±15，对候选人质量的区分度更高
- **简历评估替代一次评估**：导入完整简历后，二次评估直接替代一次评估的调整值（不再累加），以简历评估结果为准；详情弹窗的维度评分同步展示简历二次评估结果

## v2.15.2 — 打招呼与界面优化

### 新增功能

- **命令行打招呼优先使用上下文直发**：命令行模式打招呼优先使用已缓存的候选人上下文（jid/lid/securityId）直接发送，减少页面依赖；需人工确认的候选人支持清除待确认状态重新操作

### 体验优化

- **按钮图标尺寸优化**：按钮图标从 20px 增大至 24px，视觉更清晰；图标超采样逻辑统一由缓存层处理
- **搜索高亮样式优化**：搜索匹配高亮改为深青加粗，替代原来的红色文字，视觉效果更清晰

### 问题修复

- **首页描述补充学历核验**：首页功能特性说明补充学历证书核验功能描述
- **搜索高亮行高一致**：搜索匹配高亮的加粗字号继承表格基础字体，避免行高不一致

## v2.15.1 — 一键 AI 评估

### 新增功能

- **一键 AI 评估**：筛选结果、数据统计看板右键支持单个候选人 AI 评估、多个候选人批量 AI 评估（只适用未 AI 评估过的候选人）

### 问题修复

- **修复搜索框不自动滚动**：筛选结果页输入搜索关键词后，自动滚动到第一个匹配行并选中

## v2.15 — 岗位配置解析优化

### 新增功能

- **岗位需求解析更准确**：硬约束识别拆分强/弱指示词，避免「需要」「要求」等高频词误判；支持段落标题模糊匹配（如「应聘条件」「基本要求」「期望你」）
- **AI 增强解析异步化**：正则解析结果秒填界面，AI 后台增强完成后自动更新，解析过程显示动态进度指示器
- **原文溯源**：技能关键词和必要条件列表可查看原文出处，鼠标悬停显示完整来源段落

### 体验优化

- **代码质量优化**：清理冗余代码、消除重复逻辑、提升解析效率

## v2.14.2 — 学历核验浏览器连接修复

### 问题修复

- **修复学历核验浏览器启动超时**：创建浏览器页面时统一使用自动端口分配，不再等待默认 9222 调试端口，避免端口占用时连接超时失败

## v2.14.1 — 学历核验独立模型与 AI 评估超时调节

### 新增功能

- **学历核验可指定独立模型**：系统设置的已保存模型列表中，右键可将某个模型设为学历核验专用模型，AI 评估继续使用全局模型，无需手动切换
- **AI 评估超时可调**：运行控制页支持调节 AI 评估读取超时（10–300 秒），适配不同网络环境和模型响应速度
- **同版本内容更新检测**：重新打包同版本号时，更新检查会识别文件内容变化并提示更新，支持 Windows 和 macOS

### 体验优化

- **已保存模型列表优化**：新增学历核验标记列，服务商、状态等列加宽显示更完整，超出时鼠标悬停显示完整内容
- **关于弹窗更新**：功能描述更准确，新增 Gitee 镜像链接和作者信息
- **学历核验界面优化**：状态提示文字更准确

## v2.14 — 学历证书核验

### 新增功能

- **新增学历证书核验**：支持批量导入毕业证书图片或 PDF，识别姓名、证书编号、学校和专业，并通过 Chrome 打开学信网完成批量查询、验证码识别与失败重试

### 体验优化

- **中转服务更稳定**：中转服务读取超时从 60 秒延长至 120 秒，并发数从 3 降至 2，减少大模型响应慢或网关拥塞导致的超时失败

## v2.13.1 — 打招呼状态跟踪与 AI 评估稳定性提升

### 体验优化

- **打招呼状态跟踪更可靠**：发送结果无法确认时记录为待确认状态，避免重复发送；后续扫描自动跳过待确认的候选人，核实后可继续操作
- **连续发送异常时自动停止**：打招呼连续多次结果不明确时自动暂停，提醒人工核实后再继续，避免批量无效操作
- **打招呼确认等待更充分**：延长发送后的状态确认等待时间，减少因页面响应慢导致的误判
- **AI 评估更稳定**：中转服务自动降低并发数，减少网关拥塞导致的失败；网络异常采用阶梯式重试间隔，恢复成功率更高

## v2.13 — AI 模型兼容与批量候选人操作

### 新增功能

- **多选批量处理候选人**：筛选结果页和明细窗口支持多选候选人，右键可批量打招呼、移除或导出；批量沟通会按候选人发送条件分组处理并保留执行结果
- **扩展 AI 服务兼容范围**：支持 Anthropic、Azure OpenAI 和常见 OpenAI 兼容服务，模型连接测试会验证能否生成程序可识别的评估结果

### 体验优化

- **候选人信息补全范围扩大**：单次最多补全 20 页候选人信息，提高大量候选人场景下的资料完整度
- **扫描完整性提示更明确**：达到扫描或信息补全上限时，如最后一轮仍有新增或有效命中，日志会提示结果可能不完整
- **AI 评估统计更透明**：评估结束显示成功数、失败数和成功率，失败率过高时提醒人工复核
- **模型兼容性验证更可靠**：连接测试会实际验证评估结果格式，并显示完整兼容或兼容模式；不支持结构化输出的模型会自动降级处理
- **筛选完成口径更准确**：分别显示规则筛选通过人数、AI 复核淘汰人数和最终保留人数，避免将最终人数误写成规则通过人数
- **已保存模型列表优化**：模型名称、服务商、状态、Base URL 四列始终显示，Base URL 过长时鼠标悬停显示完整内容；窗口最大化时列宽自动拉伸
- **批量操作反馈更完整**：模型测试和批量沟通会显示响应时间、停止状态，以及认证失败、配额超限、免费额度不足或模型未开通等原因，方便定位配置或页面问题
- **统招本科识别更准**：本科就读起止时间满 4 年的候选人自动通过学历硬条件，不再因学历形式疑似非统招误进人工确认
- **在职候选人减少人工确认**：求职状态为在职的候选人直接通过状态检查，不再标记为需人工确认

### 问题修复

- **修复学历经历误判**：第一学历明确为大专或专科后取得本科、专升本及本科教育不超过三年的候选人按岗位硬条件淘汰，不再误进入人工确认
- **修复 AI 返回格式异常导致批量评估失败**：通义千问评估改用稳定的参数化结果返回，并关闭不必要的深度思考；其他模型仍保留格式修复和纠错重试
- **修复部分服务商协议不兼容**：支持 Anthropic 和 Azure 的接口差异，统一处理 OpenAI 兼容服务，并按具体模型保存已验证的输出能力
- **修复启动时偶发页面断线误报**：浏览器刷新或重建页面导致旧连接失效时，跳过本次选择器检查并等待自动重连，不再误报选择器异常
- **修复剩余沟通次数被误判为已用完**：收紧沟通限制提示关键词，只在页面出现明确的次数耗尽提示，或可见升级弹窗同时包含沟通次数说明时停止发送；“今日剩余 2 次”等正常额度提示不再触发上限警告
- **修复点击打招呼后直接记为成功**：列表页发送后会确认按钮变为“继续沟通”或出现明确成功提示；页面状态无法确认时停止本轮发送且不写入已沟通状态，避免漏记或重复发送
- **修复已保存模型状态不显示**：首次配置或切换模型后，已验证的兼容状态可能未及时刷新到模型列表
- **修复切换结果页时偶发崩溃**：结果页面尚未就绪时触发刷新会导致程序异常退出

## v2.12.2 — 打招呼状态可靠性与结果页优化

### 新增功能

- **候选人明细支持完整操作菜单**：首页和统计明细窗口可按姓名、匹配分查看详情，支持导入简历做二次评估、更新跟进、标记反馈、手动打招呼、加入黑名单、移除或导出候选人

### 体验优化

- **筛选结果统计更直观**：统计卡片改为强烈推荐、推荐、待定、已打招呼四类，首页和数据统计页统一使用“通过筛选”口径和图标
- **AI 评估日志更紧凑**：每位候选人的分数变化、结论和理由摘要集中在一行展示，完整评估理由仍保留在候选人详情和 Excel 中
- **上下文保存状态更明确**：首次获取候选人发送信息时显示“已保存”，已有信息成功替换时显示“已刷新”
- **重复扫描提示更准确**：筛除当前岗位已沟通候选人后，日志改为显示“本轮待评估”人数，不再将历史未沟通候选人误称为新增
- **打招呼准备更充分**：优先为待定、需人工确认和超出单轮自动沟通上限的候选人保存发送所需信息，后续手动沟通更顺畅
- **状态提示更易理解**：结果表只展示业务状态；需要切换推荐页面时会说明原因，点击“确定”后继续，不再暴露内部技术信息

### 问题修复

- **修复硬条件信息不足时误判通过**：工作经验无法确认、只有本科层级但缺少统招证据时改为“需人工确认”，明确应届、经验不足或非统招学历时直接淘汰
- **修复 AI 判定淘汰后仍保留候选人**：AI 发现的硬条件问题必须提供原文证据并通过规则复核；确认淘汰或评分低于门槛后不再进入后续沟通队列
- **修复打招呼成功后状态偶尔丢失**：发送成功后立即保存本地状态，并在扫描结束或中断时合并最新数据，避免旧数据覆盖新状态
- **修复浏览器连接并发冲突**：自动检测和手动沟通同时连接浏览器时改为串行处理，避免偶发连接异常
- **修复未打开运行页时日志处理异常**：运行日志控件尚未创建时保留待显示内容，不再报错或丢失日志

## v2.12.1 — 结果表自适应列与导出优化

### 新增功能

- **筛选结果表自动适配窗口**：普通窗口显示 8 列，最大化时显示 11 列（新增学历、年龄、求职状态），窗口够宽时显示 13 列（新增学校、公司）
- **Excel 导出文件名更直观**：选中导出以候选人姓名命名，全量导出以岗位名称和日期范围命名
- **选中导出改为 Excel 格式**：不再导出 JSON，直接生成 Excel 文件，方便查看和分享
- **明细窗口状态提示**：点击统计数字打开的明细窗口中，状态内容过长时鼠标悬停可查看完整信息

### 体验优化

- **配置大模型后自动启用 AI 评估**：启动时检测到已配置模型，自动勾选 AI 评估选项，无需手动开启
- **程序启动更快**：减少启动等待时间，界面更快可用
- **表格悬停更流畅**：优化信息显示逻辑，鼠标滑过表格行时不再卡顿
- **扫描更高效**：减少不必要的网络请求，节省接口调用额度

## v2.12 — 打招呼上下文持久化

### 新增功能

- **手动打招呼不再要求浏览器停留在推荐页**：筛选阶段已保存打招呼所需参数，可随时直接发送；发送失败自动回退到列表按钮，确保打招呼不中断
- **打招呼参数持久化保存**：参数随候选人数据保存，重启程序后仍可直接发送

### 体验优化

- **上下文采集更聚焦**：只对高分候选人采集打招呼参数，降低被平台风控的概率
- **"需人工确认"候选人也能直接打招呼**：提前采集参数，核实通过后立即可用
- **扫描覆盖率提升**：单次扫描候选人信息更完整
- **扫描日志更清晰**：完成时显示各来源贡献人数
- **Excel 导出更稳**：自动过滤候选人信息中的异常字符，避免个别数据导致导出失败

### 问题修复

- **统计看板明细与汇总日期一致性**：明细列表现在按所选日期范围过滤，与上方汇总卡片口径一致
- **岗位名匹配容错**：选择岗位时容忍空格和大小写差异，避免参数与配置文件不一致时静默空跑
- **工作年限解析更准**：避免把毕业年份（如"2015年"）误识别为工作年限，也避免跨行文本造成误判

## v2.11.4 — 三层扫描逻辑重构

### 体验优化

- **扫描节奏全面放宽**：API 补全预算从 80 提高到 160（默认最多补全 8 页）；单轮打招呼上限从 20 人放宽到 50 人，批次大小从 6 人放宽到 10 人；批次暂停和 API 分页间隔缩短，整体节奏更接近熟练人工操作
- **移除安全扫描开关**：三层扫描逻辑统一后不再区分安全扫描和 API 直调两种模式，运行控制页移除"安全扫描"复选框和"读取上限"输入框
- **岗位校验更严格**：刷新后岗位标识变化时弹窗提醒用户切回目标岗位，不再静默继续扫描；job_id 提取支持更多参数名，title fallback 采用保守判断

### 问题修复

- **修复无法打招呼和筛选的问题**：上一版本 API 优先的提取链路会导致候选人数据不完整，自动和手动打招呼均无法正常执行；重构为三层扫描逻辑（listener 补全首屏 → DOM 滚动建集合 → API 后置补全结构化字段），确保自动打招呼和筛选只作用于页面实际可见的候选人

## v2.11.3 — DOM 可点击扫描与页面跑偏检测

### 新增功能

- **结构化补全说明**：图形界面运行控制页固定展示 `listener + refresh → DOM → API 兜底` 的结构化补全策略，不再暴露"安全扫描"开关；运行日志只记录 listener 捕获数量和 DOM 命中数量
- **API 补全预算**：API 直调仅用于补全 DOM 已出现候选人的结构化字段，默认补全预算从 80 提高到 160，对应默认最多补全 8 页；补全页数按 `max_candidates` 预算推导但上限为 10 页，按 `page_num` range 范围逐页翻取且页间保留随机间隔
- **页面跑偏检测**：扫描和打招呼前自动检查浏览器是否仍在推荐牛人页面，页面被意外切换时停止本轮运行并提醒切回

### 体验优化

- **提取链路默认更稳**：默认先用 listener + refresh 捕获同页 API 结构化数据，再用 DOM 滚动确认可点击候选人，最后只对仍缺结构化字段的 DOM 候选人做 API 兜底；结构化数据只按 `geek_id` 合并，API 返回的额外候选人不会进入筛选和自动打招呼队列
- **DOM 扫描下节奏放宽**：DOM 可点击扫描成为主路径后，单轮自动打招呼上限从 20 人放宽到 50 人，批次大小从 6 人放宽到 10 人；批次暂停缩短为约 3-8 秒随机间隔，API 兜底分页间隔缩短为约 2-4 秒
- **页面上下文更一致**：listener + refresh 后会校验岗位标识，发现刷新后岗位变化会提示切回目标岗位；验证码和页面提示通过 `captcha_callback` / `notice_callback` 传递，等待循环使用 `done.wait(timeout=0.5)` 保持界面可停止；API 遇到 403/412/429 等风控状态只停止结构化补全，不丢弃已通过 DOM 提取的候选人
- **结构化日志更聚焦**：运行日志只保留 listener/API 捕获数量、命中 DOM 数量和分页兜底结果，清除接口地址、jobId、identity、API URL 探测等临时诊断输出
- **运行控制页简化**：移除旧安全扫描开关、读取上限输入框、读取上限滚轮处理（mousewheel）和安全扫描联动控件（removed def `_update_safe_scan_controls`），界面只保留 DOM 滚动轮次、岗位选择、自动打招呼和 AI 评估
- **更新日志远端修正**：更新日志弹窗现在从远端 Release Notes 获取当前版本说明，本地内容先显示、后台自动更新；读取失败时静默保留本地内容

## v2.11.2 — 反风控增强

### 新增功能

- **安全验证弹窗处理**：打招呼时自动检测验证码/安全验证弹窗，提示用户手动完成验证；验证完成后停止本轮自动打招呼，避免连续动作继续触发风控
- **自动打招呼上限提醒**：每轮自动打招呼最多 20 人，达到上限时弹窗提示已处理人数、剩余人数，以及下次扫描会自动跳过已打招呼候选人继续处理
- **验证码诊断包**：遇到验证码时自动保存诊断 JSON，并尽量保存页面截图，方便后续根据用户现场分析弹窗形态和选择器变化

### 体验优化

- **候选人提取链路更保守**：优先使用 API 直调分页；只有 API 直调失败后才启动 API listener 并刷新页面；仍失败时再回退 DOM 提取，减少不必要的刷新和监听动作
- **API 风控状态立即停止**：接口返回 403、412、429 等明确风控状态时立即停止本轮扫描，不再继续刷新页面或 DOM 兜底，避免扩大风险
- **API 分页和打招呼节奏放缓**：API 分页增加随机间隔；自动打招呼按批次暂停，整体节奏更接近人工操作
- **打招呼顺序更稳定**：自动打招呼按页面扫描顺序点击，不再按评分重排点击，降低虚拟列表大幅跳转带来的失败和风控风险
- **验证码识别范围扩展**：补充极验、captcha、verify 等常见安全验证选择器，提高弹窗识别覆盖率
- **更新日志远端修正**：更新日志弹窗先显示本地内置内容，再后台读取当前版本远端 Release Notes；读取成功后自动替换当前版本说明，失败时静默保留本地内容

### 问题修复

- **AI 评估状态显示修复**：运行控制页首次打开时 AI 评估状态显示不准确，现在正确反映 API Key 配置状态

## v2.11.1 — 界面启动与稳定性补丁

### 新增功能

- **简历导入格式扩展**：简历二次评估新增 TXT、Markdown、RTF、HTML 格式支持，纯文本和 HTML 无需额外依赖，RTF 需安装 striprtf（仅 22KB）
- **统计时间筛选扩展**：数据统计看板新增"本月"时间范围，方便按月查看筛选效果

### 体验优化

- **启动和切页更顺畅**：启动后只创建首页，运行控制、筛选结果、统计和模型配置页按需创建并错峰预热，减少首次进入重页面的卡顿
- **筛选结果页体验优化**：筛选结果页后台预热，真实点击时优先复用已创建页面；日期筛选控件保留下拉日历，不再退化成普通文本输入
- **模型配置页减少等待**：启动和首次打开模型配置页时不再同步等待系统钥匙串，API Key 改为后台读取后回填
- **窗口布局可配置**：新增 `ui_config.json`，用于统一管理窗口尺寸、页面间距、表格行高和部分控件宽度，方便不同屏幕下微调界面
- **全屏布局优化**：表单类页面（岗位配置、运行控制）内容宽度设上限，大屏居中显示不拉伸；结果页和统计页保持全宽利用
- **高窗口自动扩展行数**：窗口高度接近全屏时，岗位配置页的招聘需求文本框、技能列表和已保存模型列表自动增加可见行数，减少滚动
- **页面切换更跟手**：切换页面时延迟加载数据（首页统计、筛选结果、统计看板），不再在切页瞬间阻塞主线程

### 问题修复

- **退出保存更稳**：异常退出、中断或停止时自动保存进度并导出 Excel；停止打招呼时立即响应，不再继续处理剩余候选人
- **筛选条件修复**：修正”面议”出现在非薪资字段时薪资判断被跳过的问题；岗位未设年龄上限时不再默认限制 35 岁；硕士/博士学历含非统招关键词时标记待人工确认，不再无条件通过
- **技术条件匹配修复**：技术条件改为精确匹配，避免短关键词（如”Go”、”C”）误命中无关文本
- **简历评估修复**：AI 评估和简历二次评估响应停止请求；超长内容自动截断，防止超出模型上下文限制导致报错
- **数据恢复更稳**：候选人数据损坏时优先从备份恢复；去重合并保留最新的反馈和跟进记录
- **更新流程更稳**：修正特殊路径或字符导致的更新脚本异常，版本比较逻辑统一
- **扫描流程更稳**：接口分页结束后继续提取剩余候选人；异常中断时避免二次崩溃
- **筛选解析修复**：工作年限不再误识别毕业年份（如”2015年”）；薪资识别支持多行文本和”万”单位
- **macOS 配置文件补全**：首次从 DMG 安装后自动复制界面配置文件，修复布局异常

## v2.11 — 黑名单与简历二次评估

### 新增功能

- **候选人黑名单**：筛选结果和明细窗口支持将候选人加入黑名单，记录屏蔽原因；后续扫描按 `geek_id` 跨岗位跳过该候选人，统计和 Excel 导出也不再计入
- **简历二次评估**：支持导入候选人 PDF/Word 简历，用完整简历再次调用 LLM 评估并叠加调整分；结果页、详情页和 Excel 导出同步展示二次评估分数与理由
- **显示已屏蔽候选人**：结果页新增开关，可临时查看已加入黑名单的候选人，并支持移出黑名单

### 体验优化

- **黑名单弹窗布局优化**：加入黑名单弹窗改为更宽的业务弹窗，屏蔽原因使用提示文本引导填写，确认/取消按钮与关于弹窗保持同一套自绘按钮风格
- **安装包更轻**：简历 PDF 解析从 PyMuPDF（50MB）换为 pdfminer.six（~20MB），体积更小且中文简历解析更准确

### 问题修复

- **命令行版本显示修复**：CLI 启动横幅改为读取 `gui_main.py` 中的版本号，不再显示旧的硬编码版本
- **黑名单数据保留修复**：清空候选人时保留已屏蔽记录，避免黑名单被误清后再次刷到同一候选人

## v2.10.3 — Excel 导出依赖瘦身

### 体验优化

- **Excel 导出依赖更轻**：Excel 文件改为使用 `openpyxl` 直接写入，不再依赖 `pandas`，源码运行和打包环境需要安装的依赖更少
- **安装包体积更稳**：打包时排除 `numpy`/`numpy.libs`，避免仅因历史环境残留或可选依赖把大型科学计算库打进安装包

### 问题修复

- **打包依赖检查同步**：发布前依赖检查同步移除 `pandas`，并增加测试锁定 Excel 导出不再回退到 pandas/numpy 依赖链

## v2.10.2 — API 直调提取 + 多级兜底

### 新增功能

- **API 直调候选人提取**：默认从当前推荐页岗位参数直接调用 BOSS 推荐接口分页获取候选人，优先使用接口 JSON 中的结构化履历数据，减少对页面滚动和 DOM 可见文本的依赖
- **多级提取兜底链路**：API 直调不可用时自动降级到 `listener + page.refresh()` 获取首屏接口数据；刷新监听也不可用时继续回退到 DOM 提取，保证接口变化时仍可运行

### 体验优化

- **刷新兜底更安全**：备用刷新方案会比较刷新前后的岗位标识；如刷新后岗位变化，停止扫描并提示先将目标岗位设为默认岗位，避免静默抓取错误岗位的候选人
- **API 结构化数据全链路复用**：接口获取到的完整候选人摘要继续写入 JSON、Excel 详细信息、AI 二次评估输入和候选人详情页，不新增重复摘要字段
- **Excel 导出增强**：列重排为 5 个区块（身份→画像→评估→跟进→原始），薪资、经验、年龄等字段优先使用接口结构化数据；颜色标识改为动态查找列位置
- **GUI 双击查看详情**：主结果表和明细窗口均支持双击弹出候选人详情
- **评分拆解显示 AI 调整**：评分拆解新增 AI 增减分项，一目了然看到大模型对规则评分的修正
- **明细窗口布局优化**：宽度限制在主窗口内，状态列加宽；统计看板岗位名称列加宽至 200px
- **启动更快**：移除开始运行时的 3 秒等待

### 问题修复

- **API 监听稳定性修复**：修复微滚动只能捕获少量候选人接口数据的问题；当直调失败时，刷新监听作为受保护的备用路径获取完整结构化数据
- **薪资解析兼容性增强**：兼容更多无后缀薪资格式（如"15-25"），薪资文本解析逐字段兜底，不再因其他字段有值就跳过

## v2.10.1 — API 候选人提取

### 新增功能

- **API 候选人提取**：支持从 BOSS 推荐接口 JSON 直接提取候选人，不再完全依赖页面滚动获取，提升数据完整性和提取速度

### 体验优化

- **候选人信息正确显示**：修复薪资、工作年限、教育经历在 API 格式下解析错误的问题，支持多学历展示
- **运行页职位不再重置**：移除 API 监听预热时的页面刷新，改为微滚动，避免重置用户手动选择的招聘职位
- **淘汰原因归类合并**：筛选淘汰原因按类别（经验/学历/年龄/地点/薪资/技术条件）合并统计，不再逐条列出
- **AI 评估进度即时更新**：提交评估任务后立即显示进度，不再等第一个结果返回；评估日志格式统一
- **筛选完成状态标识**：进度条最终状态显示自绘彩色图标（绿色勾/红色叉），附带通过数/总数/已打招呼统计
- **选择器检查静默化**：连接后自动检查选择器，一切正常时不输出日志，仅异常时提示
- **学历筛选更合理**：疑似非统招本科不再直接淘汰，改为标记风险提示，确认后再决定是否沟通
- **Excel 导出增强**：新增"是否需人工确认"、"风险提示"、"自动打招呼阻断原因"三列，方便离线审阅
- **候选人详情展示风险**：详情页和打招呼确认弹窗展示风险提示，避免误操作
- **AI 评估更精准**：传给大模型的候选人信息经过结构化压缩，评估质量和响应速度均有提升
- **AI 评估更稳定**：调整网络超时参数，减少因网络波动导致的评估失败
- **API 提取数据更精准**：候选人经验、年龄、期望城市、期望薪资等关键信息直接从接口数据传递，不再依赖文本正则重新解析，筛选结果更可靠
- **薪资筛选更稳定**：兼容更多薪资格式（如"15K以上"），减少因格式差异导致薪资过滤被跳过
- **多城市候选人匹配**：期望多个城市的候选人不再被误判为"地点不符"

### 问题修复

- **启动崩溃修复**：修复部分环境下 GUI 启动时因状态图标初始化顺序错误导致崩溃的问题

## v2.10 — 筛选复盘闭环

### 新增功能

- **筛选结果可解释**：候选人记录新增评分拆解、评分解释和关键词命中证据，详情页和 Excel 导出都能查看分数来源
- **人工反馈闭环**：筛选结果支持标记合适、误推、误杀、放弃和备注，反馈会写入候选人数据、详情页和 Excel 摘要
- **候选人跟进状态**：筛选结果支持更新未沟通、已打招呼、已回复、待约面、已约面等跟进状态，并同步到详情页和 Excel 摘要
- **数据统计复盘指标**：岗位明细新增已反馈、合适率、误推率、已回复、回复率、已约面和约面率，便于判断筛选质量和跟进转化

### 体验优化

- **更新失败通知**：启动后检测上次自动更新是否完成，未完成时弹窗提示并可手动重试
- **旧版本备份保留**：Windows 更新成功后保留 .old 文件，便于新版本异常时快速恢复
- **更新来源追踪**：更新日志记录触发来源（startup/manual），便于排查问题
- **启动检查延迟**：自动更新检查延迟调整为 12 秒，避开 GUI 初始化和 PyInstaller 释放窗口

### 问题修复

- **候选人标题样式**：修复详情页候选人姓名标签背景和字体不一致的问题

## v2.9.3 — 需求解析精度优化

### 体验优化

- **AI 解析更稳定**：网络不稳定或模型响应慢时，会自动重试或回退到本地解析，不再直接报错
- **解析结果更精准**：泛化词（如"人工智能"、"证券行业"、"数据清洗"等）不再被误识别为技能关键词
- **基础条件自动归类**：学历要求、工作年限等基础条件自动归入基本信息，不再混入必要条件
- **优先项权重更合理**：优先项（如"证券经验优先"）的加分权重不再被过度放大
- **弹窗模块边界更清晰**：关于弹窗、版本历史和更新弹窗共用更新日志解析与渲染逻辑，减少重复代码

### 问题修复

- **AI Agent 变体匹配**：修复"智能体"、"大模型 Agent"、"Langchain"等写法无法正确匹配的问题
- **工作地点匹配**：修复 AI 返回的地点格式不统一导致筛选失败的问题
- **Mac 筛选结果按钮位置**：修复筛选结果页"重置日期"按钮位置不协调的问题
- **Mac 更新弹窗按钮不可见**：修复 Mac 上更新提醒弹窗底部按钮被遮挡的问题

## v2.9.2 — 模型管理增强 + 运行控制优化

### 新增功能

- **AI 增强招聘需求解析**：解析招聘需求时先用正则生成基础配置；当前模型和 API Key 可用时，再由 AI 基于原文和正则初稿补充优化基本信息、技能关键词、优先项和必要条件；未配置 AI 或调用失败时自动回落到正则解析
- **岗位优先项独立加分**：招聘需求中的"证券行业优先"、"全栈开发经验优先"、"大模型 Agent 经验优先"等优先项会保存为 `preferred_keywords`，作为额外加分项参与排序，不再稀释普通技能关键词匹配分
- **必要条件 OR 解析**：硬性条件中的"金融投资行业经验，债券、基金、期货、期权等"会解析为 OR 条件，候选人满足任一方向即可通过，并支持"固收/固定收益"等行业别名匹配
- **已保存模型连通性测试**：已保存模型列表右键菜单新增"测试连通性"选项，可快速验证模型可用性
- **多接入方式支持**：API Key 按 provider+base_url 组合存储，同一服务商可同时配置 API 和 Token Plan 等多种接入方式

### 体验优化

- **新建岗位全流程呼吸提示**：岗位配置页新增三处动态提示（点击查看需求示例→、←点击解析招聘需求、点击保存配置→），与"点此新增岗位→"风格统一（绿色呼吸动画），按流程阶段依次出现，完成后自动消失
- **保存提示更精准**：新建流程中，"点击保存配置→"提示等检查结果步骤打勾（滚动到底部）后才出现，避免过早引导
- **按钮文字优化**："解析需求文档"改为"解析招聘需求"，步骤文字"② 解析文档"改为"② 解析需求"
- **解析摘要增加薪资**：解析成功后显示的摘要信息新增薪资范围，格式为"薪资=12-15K"、"薪资≥12K"或"薪资≤15K"
- **需求示例提示交互**：点击"招聘需求示例"按钮后，左侧"点击查看需求示例→"提示自动消失
- **服务商默认模型更新**：DeepSeek、Kimi、智谱、MiniMax、小米、阶跃星辰、OpenAI、Anthropic 等服务商默认模型更新为最新版本
- **MiniMax Base URL 更新**：从旧域名 minimaxi.com 更新为 api.minimaxi.com
- **运行控制页优化**：打招呼等级备注动态显示实际阈值，AI 评估备注 +/- 分色显示并默认勾选，岗位选择提示文本优化
- **发布传输策略优化**：GitHub/Gitee 上传下载改为大文件串行、小文件并发；对端产物优先用 GitHub digest 和 latest.json 判定可复用，Windows 发布同步 Mac 产物时 ZIP 优先于 DMG，减少重复传输并提升网络波动下稳定性

### 问题修复

- **服务商切换配置丢失**：修复切换服务商时 API Key、Base URL、模型名称显示为默认值而非已保存配置的问题
- **已保存模型切换崩溃**：修复 use_selected_model 中 provider 变量未定义导致崩溃的问题
- **右键菜单字号不统一**：修复 Entry/Text 控件右键菜单字号（14pt）与其他菜单（12pt）不一致的问题

## v2.9.1 — 模型管理增强 + 更新弹窗优化

### 新增功能

- **模型下线检测**：获取模型列表时自动对比上次结果，检测已下线的模型并弹窗提醒
- **批量连通性测试**：选择模型对话框支持多选（Ctrl+点击切换、Shift+点击范围、Ctrl+A 全选），右键菜单可批量测试连通性，多线程并行执行
- **业务错误友好提示**：识别模型未开通、配额超限、免费额度用完等常见错误，给出人性化提示和解决方案

### 体验优化

- **更新弹窗重构**：适配 DPI 缩放，按钮使用原生控件，更新内容从远端 CHANGELOG 提取与主界面版本历史完全一致
- **测试连接提示优化**：系统设置页测试连接也统一使用人性化提示，不再显示技术细节
- **批量测试结果精简**：只显示汇总数（X 个可用，Y 个不可用），失败模型灰色显示不突兀
- **API Key 读取优化**：修复测试连通性时 provider 显示名称与内部键不一致导致 keyring 查不到 Key
- **模型名称解析优化**：修复选择模型时连通性测试的状态后缀被当成模型名导致 API 调用失败

### 问题修复

- **Windows EXE 版本号修复**：修复打包时版本号显示为 2.9.0 的问题

## v2.9 — 模型管理增强 + 新建岗位引导

### 新增功能

- **模型列表搜索**：选择模型对话框内置搜索框，输入关键词实时过滤模型列表，快速定位目标模型
- **新增模型检测**：获取模型列表时自动对比上次结果，新增模型以绿色高亮显示并弹窗提醒
- **新建岗位步骤引导**：岗位配置页点击新建后显示 4 步流程引导（填入需求→解析文档→检查结果→保存配置），每步完成后自动推进，滚动到底部即进入保存步骤

### 体验优化

- **当前模型显示优化**：系统设置页"当前使用模型"显示中文服务商名称（如"通义千问 (Qwen) / qwen3.7-max"），与其他页面风格一致
- **新建岗位引导提示**：岗位配置页新建按钮左侧显示呼吸动画提示"点此新增岗位→"，点击后自动消失

## v2.8.12 — 界面细节打磨 + 筛选默认值优化

### 体验优化

- **筛选结果日期默认一周**：日期过滤默认显示最近一周的结果（起始一周前、终止今天），重置按钮也恢复为这个范围，减少手动选日期的操作
- **发布流程更高效**：本地打包改用构建指纹判断，源代码、配置、依赖和打包参数都未变化时跳过 PyInstaller；上传 GitHub/Gitee Release 文件前先校验远端同名文件，内容完全一致时自动跳过；当前平台更新包未变化时不触发跨平台 CI；`latest.json` 无变化时不再提交和推送
- **API 服务商名称优化**：系统设置页的服务商下拉列表显示中文名称（如"通义千问 (Qwen)"、"月之暗面 (Kimi)"），更易识别；MiniMax 的 API 地址更新为新域名；部分服务商的默认模型更新为最新版本
- **发布进度表更详细**：发布完成后的汇总表展示每个子步骤的名称和耗时，方便定位瓶颈

### 问题修复

- **弹窗背景色统一**：修复"选择模型"和"清空候选人"弹窗在 macOS 上文字和按钮出现白色方块的问题；Windows 不受影响
- **升级后启动兼容**：修复部分电脑升级后因日历控件缺失导致 App 启动报错的问题；打包前也会提前检查日历依赖是否完整，并校验依赖清单没有遗漏
- **Windows 自动更新重启修复**：修复更新完成后自动重启时可能继承旧版运行时目录，导致提示 `python312.dll` 缺失的问题；新版本文件已安装成功时手动启动不受影响
- **跨平台 CI 误触发修复**：修复重新发布同一版本时，仅修改 build.py 等非构建文件也会触发 CI 重建对端产物的问题；现在只有修改核心模块（gui_main.py、bossmaster.py 等）才会触发 CI 重建

## v2.8.11 — 筛选结果增强 + 需求解析优化

### 新增功能

- **筛选结果日期过滤**：筛选结果页新增日期范围过滤，点击日历控件选择开始和结束日期，结合岗位下拉框实现双重过滤，快速定位特定时间段的筛选结果；两个日期控件互斥关闭，避免同时弹出；终止日期自动校验不小于起始日期、两个日期均不能超过今天

### 体验优化

- **卡片视觉统一**：所有功能卡片标题行增加蓝色竖线装饰，运行日志区域统一为卡片风格，与浏览器状态卡片视觉一致
- **明细弹窗优化**：标题和统计信息字号更紧凑，表格列头加粗，弹窗背景与页面统一，消除白色方块；右键菜单字体缩小更精致
- **候选人详情窗口放大**：查看详情窗口扩大，内容显示更充分
- **需求输入框占位提示**：岗位配置页输入框空时显示灰色提示文字，点击自动消失、清空自动恢复
- **页面切换更流畅**：优化页面切换性能，切换时不再有明显卡顿
- **字体统一**：岗位配置编辑区技能名称字体与页面其他区域保持一致
- **招聘需求解析更智能**：支持更多薪资格式（年薪、无标签、单边下限）、年龄格式（不超过、周岁以内、≤符号）、经验写法（中文数字、至少/不低于）；非标准段落标题（岗位职责、任职要求等）也能正确识别；从网页或微信复制的内容（全角字符、emoji）不再解析失败

### 问题修复

- **Windows 自动更新更可靠**：更新失败时会自动重试一次，降低更新中断的风险；同时清理残留临时文件，避免旧版本干扰
- **技能关键词误匹配修复**：修复 Go、API 等短词误匹配 Google、RAPID 等无关单词的问题
- **技能权重分配修复**：修复"精通"、"优先"、"熟练"等上下文关键词无法识别导致技能权重始终为默认值的问题

## v2.8.10 — 界面体验打磨

### 体验优化

- **界面字体更统一**：表单标签、按钮、下拉框等常用控件统一使用同一套字体，减少不同区域字体大小不一致的情况
- **候选人明细弹窗优化**：点击统计数字弹出的明细窗口高度增加，方便查看更多候选人；"已打招呼"人数用绿色醒目显示，一眼区分
- **筛选进度条更美观**：运行时进度条高度与右侧百分比文字对齐，不再高低不齐
- **AI 评估勾选框加大**：运行控制页"启用 AI 辅助评估"勾选框加大，鼠标更容易点中
- **表格表头统一加粗**：岗位配置的技能关键词列表、筛选结果列表、数据统计的岗位明细表，三处表头统一为加粗样式
- **侧边栏更紧凑**：左侧导航栏字体缩小，同屏可显示更多导航项，减少滚动
- **需求输入框更醒目**：岗位配置页"粘贴招聘需求文档"输入框使用白色背景 + 聚焦蓝色边框 + 灰色占位提示，与其他输入框区分更明显
- **高分屏窗口居中修复**：修复 4K 等高缩放显示器上主窗口启动时位置偏上或偏左的问题

### 问题修复

- **Mac 字体大小修复**：修复部分 Mac 环境下界面字体大小异常的问题，包括 M1/M2/M3/M4 芯片字体偏小和 Intel Mac 部分环境字体偏大；Windows 不受影响
- **Mac 界面背景色统一**：修复 macOS 上多处卡片内容区域、表单标签、下拉框和输入框背景色显示为灰色的问题；Windows 不受影响
- **侧边栏标题显示修复**：修复侧边栏 BOSS 标题字号过大导致文字显示不完整的问题
- **Windows 自动更新更稳**：更新包下载后会校验文件大小和完整性，避免下载不完整时继续安装；更新后会保留旧版本备份，新版本成功启动后再清理，便于异常时回退
- **Windows 启动兼容性优化**：调整单文件程序的运行时解包位置，减少企业电脑临时目录策略导致启动失败的概率

## v2.8.9 — 更新与数据稳定性补丁

### 新增功能

- **国内备用更新源**：新增 Gitee 更新检测，国内用户无需 VPN 也能快速检测新版本；Gitee 失败时自动回退 GitHub
- **国内下载资源补齐**：发布时同步提供 Windows、Mac、配置文件和说明文档的国内下载入口，减少用户下载失败的情况

### 体验优化

- **4K 高 DPI 显示优化**：在 4K 大屏高缩放环境下明显等比例缩小界面，减少窗口和控件偏大的感觉
- **候选人数据保存更稳**：在界面中删除候选人、清空候选人、更新打招呼状态等操作时，都会先自动备份旧数据再写入新数据，降低意外情况导致数据丢失的风险
- **下载失败自动切换**：国内下载源不可用时自动切换到备用下载地址

### 问题修复

- **候选人数据备份修复**：修复部分界面操作（如删除单个候选人、更新打招呼状态）不会自动备份旧数据的问题，现在所有数据变更都会自动备份
- **更新日志明细换行修复**：修复版本历史页个别明细行换行后只显示圆点、正文掉到下一行的问题

## v2.8.8 — v2.8 Patch

### 体验优化

- **高分辨率屏幕自动适配**：4K、2K 等高 DPI 显示器上界面大小自动调整，不再出现界面过大或过小的问题
- **窗口居中更准确**：主窗口启动时和更新弹窗在多显示器环境下正确居中，不再超出屏幕可见范围
- **Mac 安装包更规范**：DMG 双击打开后，直接拖拽应用到 Applications 文件夹即可安装
- **关于页面新增开源协议声明**：显示 MIT License 开源协议信息
- **侧边栏图标更新**：岗位配置、筛选结果、数据统计使用更匹配的图标（公文包/漏斗/柱状图），首页快速操作按钮图标保持对应
- **Logo 升级为彩色放大镜**：BOSS 标题旁的 Logo 改为高分辨率彩色放大镜图案，品牌蓝填充 + 白色高光，更醒目
- **清空候选人弹窗优化**：背景色统一、按钮带确认/取消图标、尺寸随 DPI 自适应
- **关于页面风格统一**：背景色、字体、按钮与其他弹窗保持一致，检查更新/关闭按钮带图标
- **更新日志版本列表居中**：版本号左对齐、列表块居中显示，滚动条仅内容超出时显示

### 问题修复

- **Mac 版本检测修复**：修复 Mac 应用启动时错误提示有新版本的问题
- **Mac 配置读取修复**：修复 Mac 应用无法读取岗位配置的问题，现在可以正常加载已保存的岗位规则
- **Mac 首次启动配置初始化**：修复 DMG 安装后首次启动时岗位配置为空的问题，现在会自动初始化默认配置文件
- **招聘需求模板读取修复**：修复 Mac 应用点击招聘需求模板按钮时提示找不到模板的问题
- **命令行工具配置读取修复**：修复命令行模式下配置文件读取失败的问题
- **输入框滚动修复**：修复需求文档输入框、打招呼话术框等输入区域，双指滚动时会连带滚动整个页面的问题，现在双指滑动只滚动输入框本身的内容
- **Mac 自动更新修复**：修复自动更新后程序不重启、更新后无法打开的问题
- **Mac 窗口恢复修复**：修复最小化后点击 Dock 图标无法恢复窗口的问题
- **模型列表对话框修复**：修复点击获取模型列表后对话框异常退出的问题
- **更新日志弹窗修复**：修复点击"关于"链接或"检查更新"按钮时弹窗意外关闭的问题

## v2.8 — macOS 支持

### 新增功能

- **支持 macOS**：Mac 用户可以直接下载安装包，拖到 Applications 即可使用
- **更新提醒**：启动时自动检测新版本，弹窗展示更新内容，支持一键更新

### 体验优化

- **版本号一键查看更新日志**：点击左下角版本号直接打开更新日志
- **关于页面优化**：新增 GitHub 项目地址和问题反馈链接
- **Mac 触控板滚动**：岗位配置、大模型配置等页面，双指滑动可以正常滚动了

> 注：v2.8.1 至 v2.8.7 为内部修复版本，相关改进已合并至 v2.8.8 及后续版本。

## v2.7 — AI 智能评估 + 数据统计

### 新增功能

- **AI 智能评估**：对通过筛选的候选人进行 AI 二次评估，辅助招聘决策
- **数据统计看板**：按岗位查看筛选和打招呼数据，支持时间范围过滤
- **单个候选人打招呼**：筛选结果页右键可直接对单个候选人打招呼
- **候选人详情结构化展示**：核心信息、教育背景、评分、技能匹配分层展示
- **候选人明细弹窗增强**：明细弹窗中新增查看候选人详情、移除候选人、导出功能
- **清空候选人**：支持按岗位或全部清空，可选择保留已打招呼的候选人，清空前自动备份数据
- **页面连接检查**：连接浏览器后自动检查 BOSS 页面是否正常加载，有异常时弹窗提醒

### 体验优化

- **候选人定位更准确**：补打招呼或右键打招呼时，自动滚动到目标候选人位置
- **验证码等待恢复**：遇到安全验证弹窗时暂停等待，手动完成验证后自动恢复
- **更新日志页面重构**：左右分栏浏览版本历史，点击切换内容

### 问题修复

- **更新日志显示修复**：打包后的程序可以正常查看更新日志
- **验证码检测更准确**：修复页面正常文本被误认为验证码弹窗、导致程序暂停等待的问题

## v2.6 — 筛选逻辑优化

### 体验优化

- **筛选更稳定**：筛选算法重构，评分结果更稳定可靠
- **年龄格式兼容**：候选人简历中"35岁"、"年龄：36岁"等多种年龄写法都能正确识别

### 问题修复

- **AI 模型选择保存修复**：修复保存配置后重新打开时，当前选择的 AI 模型丢失的问题
- **界面稳定性提升**：减少了偶发的界面状态异常

## v2.5 — 浏览器连接优化 + 年龄筛选

### 新增功能

- **浏览器连接更稳定**：自动分配可用端口，避免端口冲突导致连接失败；保留登录状态，无需每次重新登录
- **自动启动 Chrome**：点击连接浏览器时自动启动 Chrome，无需手动打开
- **年龄筛选**：支持配置最大年龄限制，自动过滤超龄候选人
- **学历识别更全面**：新增专升本、电大、成人高考等非统招学历类型的识别，减少学历判断遗漏

### 体验优化

- **登录状态保留**：重启浏览器后无需重新登录 BOSS 直聘
- **版本号显示统一**：窗口标题、日志、关于页面版本号一致

## v2.4 — 薪资筛选 + 必要条件配置

### 新增功能

- **薪资范围筛选**：配置岗位薪资范围，候选人期望薪资超出范围自动过滤
- **必要条件配置**：支持三种匹配模式——精确匹配单个关键词、满足任一关键词（如 activiti 或 camunda）、全部满足（如 Spring Boot 和微服务）
- **薪资输入验证**：输入非法字符时实时提示，保存时二次校验

### 体验优化

- **反爬更安全**：操作间隔随机化，降低被识别为自动化的风险
- **验证码处理**：遇到安全验证弹窗时自动暂停，提示人工处理

### 问题修复

- **数据写入更安全**：防止写入中断导致数据文件损坏
- **图标显示修复**：修复打包后程序启动时图标加载失败的问题

## v2.3 — 图标升级 + 工作地点筛选

### 新增功能

- **工作地点筛选**：配置招聘城市，自动过滤不在目标城市的候选人，支持多城市
- **招聘需求示例**：岗位配置页新增模板按钮，一键填充示例内容

### 体验优化

- **图标高清化**：全部图标替换为矢量图标，按系统缩放比例自适应，各种分辨率下都清晰
- **候选人详情弹窗优化**：弹窗居中显示，尺寸和字体放大，内容更易阅读

## v2.2 — 多岗位确认 + 性能提升

### 新增功能

- **多岗位切换确认**：切换岗位时弹出确认对话框，避免误操作
- **停止按钮即时生效**：点击停止后立即中断，自动保存当前进度
- **浏览器自动检测**：进入运行页自动检测浏览器连接状态，断线自动恢复
- **候选人去重升级**：同一候选人在不同岗位下保留独立记录
- **沟通上限检测**：达到平台每日沟通限制时自动停止，避免无效操作
- **更新日志查看**：点击版本号可查看所有版本的更新内容

### 体验优化

- **打招呼速度大幅提升**：打招呼速度提升约 12 倍
- **淘汰原因展示优化**：同类原因合并显示，附带实际招聘要求
- **多维度筛选控件**：新增工作年限、年龄、薪资范围、必要条件等配置入口
- **推荐等级阈值调整**：强烈推荐 75 分以上、推荐 65 分以上、待定 55 分以上，过滤更合理

## v2.1 — 评分模型重构

### 新增功能

- **评分模型升级**：工作经验超过岗位要求的部分每年额外加分；新增学历档次加分，博士、硕士、本科等不同学历给予不同分值
- **API Key 安全存储**：API Key 加密存储到系统钥匙串，配置文件不含明文
- **新电脑引导配置**：首次在新电脑运行时自动检测并引导重新配置 API Key

### 体验优化

- **打包体积大幅缩小**：从 931MB 缩小到 39MB
- **英文关键词更精准**：避免子串误匹配（如"AI"不再误匹配"email"）
- **找不到经验不再淘汰**：仅警告，不直接过滤

## v2.0 — 图形界面 + AI 模型配置

### 新增功能

- **图形界面**：提供完整的 GUI 界面，侧边栏导航，支持可视化配置和操作
- **AI 模型配置**：支持 10 种 AI 服务商，双击切换模型，动态获取模型列表
- **API Key 显隐切换**：点击按钮切换明文/密文显示
- **高 DPI 支持**：自适应 4K 等高分辨率屏幕

### 体验优化

- **测试连接更可靠**：连接成功率从 50% 提升到近 100%

## v1.0 — BOSS 直聘自动筛选

### 新增功能

- **候选人自动获取**：程序自动打开浏览器访问 BOSS 直聘推荐页面，自动获取候选人列表
- **智能匹配筛选**：基于工作经验、学历、技能关键词三个维度评分和排序
- **自动打招呼**：对符合要求的候选人自动发送消息，支持中断恢复
- **Excel 导出**：多工作表 + 统计摘要 + 颜色标识
- **中文数字解析**：候选人简历中"三年"、"十二年"等中文数字格式能正确识别
- **组合条件支持**：必要条件支持多个关键词灵活配置，如"满足 activiti 或 camunda 任一即可"
