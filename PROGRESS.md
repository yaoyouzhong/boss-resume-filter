# GUI 页面打开提速进度

> 历史记录：本性能任务已于 2026-07-27 结束，以下分支和测试状态只代表当次实验，不代表当前发布状态。

- 实验分支：`codex/gui-perf`；当次任务不 push、不建 PR、不打包发布。
- 目标：行为和界面零变化优先，其次数据安全，最后才是提速幅度。
- 顺序：任务 0 基线与控件树 → 启动初始化 → 首次页面 → 对拍/冒烟/回归。
- 最大风险：Tk 首次创建受进程冷启动、字体/DPI、系统负载影响，错误基线会让优化结论失真。
- 2026-07-27：恢复检查完成；此前无 `PROGRESS.md` / `BLOCKED.md`。
- 2026-07-27：创建并切换到 `codex/gui-perf`；保留管理者提供的未跟踪基准脚本，不修改。
- 2026-07-27：任务 0 第一条基准与任务书数字明显不一致，按硬规则停止；管理者随后裁决为测量口径问题，阻塞解除。
- 2026-07-27 会话基线协议：第 1 次冷跑丢弃，第 2-4 次逐项取中位数。
- 会话基线：`init=206ms`；`create home/config/run/result/stats=47/267/199/126/48ms`。
- 会话基线：`show r0 home/config/run/result/stats=268/317/3/18/7ms`；`show r1=7/7/6/2/1ms`。
- 基线三重页合计 `592ms`，总账 `893ms`；目标分别为 `≤325.6ms` 与 `≤714.4ms`，`init≤113.3ms`。
- 2026-07-27：七页递归控件树基线建立并自检全绿：`home/config/run/result/education/stats/settings=35/142/105/56/54/31/63`。
- 2026-07-27：任务 0 回归基线为稳定单元回归 `1281/0`，导入烟测通过；没有 skip/todo 或放宽断言。
- 开工回执：只优化 GUI 主线程构建路径；顺序为启动初始化、三个重页面、最终对拍与冒烟。
- 开工回执：行为和控件树必须完全不变；最大风险是把成本转移到首次切页或 after_idle 后续卡顿。
- 循环 1（已回滚）：删除 3 次无效 `update_idletasks` 后中位数 `init=255ms`（基线 206ms，+23.8%），且 `create_config=296ms`（+10.9%）；按劣化即回滚规则不保留。
- 循环 2（已回滚）：再叠加延后终态/非首页高亮图标后 `init=153ms`（基线 74.3%，未达 55%），三重页合计 `345ms`（58.3%）；热切换 r1 `14/4/9/6/5ms` 多项越过 +20% 红线。
- 循环 3（已回滚）：再延后联系清单角标读取后 `init=318ms`（基线 154.4%），热切换 r1 `24/10/14/10/9ms`；启动指标三连败，已写入 `BLOCKED.md` 并转任务 2。
- 循环 4（已回滚）：分片预建三重页时完成回调缺少 `_pending_page_ready_callbacks`，四次均 `AttributeError` 后超时；原基准中位数总账仍为基线 98.7%，且 stats 热切换劣化，未保留。
- 循环 5（已回滚）：修正预建生命周期后完整预建中位数 `2789ms`，但 `show_config=575ms`，为 config create 基线 215%，远高于 30% 目标；隐藏页首次映射仍同步布局，不搬移该卡顿。
- 循环 6（已回滚）：把配置详情容器提前登记布局后 `show_config=13ms`，但 `create_config=461ms`（基线 +72.7%），属于成本搬移；任务 2 三连败，已写入 `BLOCKED.md`。
- 2026-07-27：控件树反向验证通过：内存销毁 result 叶子后红灯 `expected 56, actual 55`（退出码 1），新进程恢复后七页全绿。
- 2026-07-27：七页逐一打开、`refresh_results(force=True)`、更新日志对话框冒烟全部 PASS，无异常输出。
- 2026-07-27：最终回归 `1281/0`，导入烟测通过；测试通过数与任务 0 基线相同。
- 会话终值：`init=217ms`；`create home/config/run/result/stats=21/94/64/40/16ms`；三重页合计 `198ms`，总账 `452ms`。
- 会话终值：`show r0=86/85/2/16/7ms`；`show r1=3/2/2/1/1ms`。
- 最终比率：init `105.3%`（目标 55%，失败）；三重页 `33.4%`、总账 `50.6%`、热切换无劣化（均通过）。
- 最终状态：6/12 个修改-验证循环；全部性能代码已回滚，仅保留基线/对拍/冒烟脚本与如实阻塞记录。

# GUI Controller 拆分进度

> 当前任务：`codex/gui-controller-modularization-03`，不 push、不合并、不发布。

- 目标：将 `gui_main.py` 降至不超过 10,500 行、`BossFilterGUI` 不超过 350 个方法。
- 顺序：候选人/结果 → 设置/工具 → 浏览器 → 联系 → 扫描 → 收尾。
- 基线：`master=b7bf96e`，17,472 行，526 方法，8 个方法不少于 200 行。
- 验证：1669/1669，导入烟测、真实 Tk、控件树、页面切换基准通过。
- 最大风险：联系、扫描和浏览器线程状态迁移时破坏现有安全终态。
- 当前：任务 0 完成；checkpoint `69d6e80` 已定义 Controller 边界，开始候选人/结果拆分。
- Checkpoint 1：结果集合读取/筛选/排序/统计迁入 `result_controller.py`；候选人简历、复评、复核、反馈、跟进、屏蔽与删除迁入 `candidate_controller.py`，兼容入口保留为薄委托。
- Checkpoint 1 指标：`gui_main.py` 16,913 行，`BossFilterGUI` 526 方法；较基线减少 559 行，方法兼容面待最终清理阶段收敛。
- Checkpoint 1 验证：1688/1688 单元回归，导入烟测、七页真实 Tk、强制结果刷新、更新日志弹窗、控件树 35/146/102/59/54/31/84 和页面切换基准通过。
- Checkpoint 1 反向验证：分别临时向结果/候选人 Controller 引入 `tkinter`，边界测试均准确单点失败；撤销违规导入后转绿。简历评估工作线程已改为 `run_on_ui()` 投递 UI 回调。
- Checkpoint 2：模型保存/目录获取、数据备份恢复/简历体检/候选人清理、学历文件导入/批量识别/学信网字段与验证码编排分别迁入 `settings_controller.py`、`data_maintenance_controller.py`、`education_controller.py`。
- Checkpoint 2 指标：`gui_main.py` 16,502 行，`BossFilterGUI` 527 方法；较 checkpoint 1 再减少 411 行，新增的模型目录 UI 结果渲染方法待最终兼容面清理时处理。
- Checkpoint 2 验证：1705/1705 单元回归，导入烟测、七页真实 Tk、结果强制刷新、更新日志弹窗、控件树 35/146/102/59/54/31/84 和页面切换基准通过。
- Checkpoint 2 反向验证：临时向设置 Controller 引入 `tkinter` 后边界测试准确单点失败，撤销后 1705/1705 转绿；模型目录工作线程统一经 `run_on_ui()` 回传普通结果。
- Checkpoint 3：Chrome 路径发现、受管 profile/动态端口启动、显式/持久化/默认端口重连、连接失败与非目标页去抖、指定调试端口进程清理迁入 `browser_controller.py`；扫描/联系决策仍留给各自领域。
- Checkpoint 3 指标：`gui_main.py` 16,260 行，`BossFilterGUI` 527 方法；较 checkpoint 2 再减少 242 行。浏览器检查兼容入口保留，最终清理阶段再收敛方法数。
- Checkpoint 3 验证：1713/1713 单元回归，导入烟测、七页真实 Tk、结果强制刷新、更新日志弹窗、控件树 35/146/102/59/54/31/84 和页面切换基准通过；Chrome 等待期间的停止信号与失败关闭语义有专门回归。
- Checkpoint 3 反向验证：临时向浏览器 Controller 引入 `tkinter` 后边界测试准确单点失败，违规导入已撤销。
- Checkpoint 4：联系清单恢复/重校验、候选人状态同步、入队/重试/人工核实，以及发送 worker 的双重发送前复核、岗位页等待、成功/失败/待核实/访问保护/意外中断状态机迁入 `contact_controller.py`；浏览器探测、实际发送和持久化仍由 GUI 显式注入。
- Checkpoint 4 线程安全：联系准备、验证码确认、队列刷新、结果刷新和终态弹窗全部改经 `run_on_ui()` 投递；发送 worker 不再调用 `root.after()`，Controller 不导入 Tk、GUI、存储、浏览器或网络模块。
- Checkpoint 4 指标：`gui_main.py` 16,055 行，`BossFilterGUI` 527 方法；较 checkpoint 3 再减少 205 行。发送 worker 由 370 行降至 236 行，兼容入口待最终清理阶段收敛。
- Checkpoint 4 验证：1725/1725 单元回归，导入烟测、七页真实 Tk、结果强制刷新、更新日志弹窗、控件树 35/146/102/59/54/31/84 和页面切换基准通过；新增 10 项联系状态机意图测试。
- Checkpoint 4 反向验证：临时向联系 Controller 引入 `tkinter` 后，完整回归准确只在联系 Controller 边界测试失败；撤销违规导入后 1725/1725 转绿。
- Checkpoint 5：运行页所有 Tk 变量在主线程冻结为不可变 `RunRequest`；扫描参数归一化、旧扫描参数对象、进度事件、stdout 日志重定向、异常归类和正常完成/轮次上限/扫描中断/停止/出错终态迁入 `run_controller.py`。
- Checkpoint 5 线程安全：`run_worker` 不再读取 Tk 变量或直接调用 `root.after()`；验证码、通知、岗位不一致、岗位配置体检和最终 UI 全部通过 `run_on_ui()` 投递，GUI 只消费普通进度/终态对象。
- Checkpoint 5 指标：`gui_main.py` 15,972 行，`BossFilterGUI` 529 方法；`run_worker` 由 345 行降至 138 行。新增参数快照与终态渲染方法使方法兼容面暂增 2，留给最终清理。
- Checkpoint 5 验证：1736/1736 单元回归，导入烟测、七页真实 Tk、结果强制刷新、更新日志弹窗、控件树 35/146/102/59/54/31/84 和页面切换基准通过；新增 9 项运行 Controller 终态/参数/日志测试。
- Checkpoint 5 反向验证：临时向运行 Controller 引入 `tkinter` 后，完整回归准确只在运行 Controller 边界测试失败；撤销违规导入后 1736/1736 转绿。
- 收尾前差距：距硬目标仍差 5,472 行和 179 方法；最终检查点必须优先清理无调用兼容入口并在仍超行数时按任务书允许拆纯样式，不能靠空壳转发凑数。
- Checkpoint 6：全仓 AST/引用审计后删除 20 个无调用兼容方法；全局 Tk/ttk 样式、字体、配色与状态图标注册迁入 `gui_style_setup.py`，`BossFilterGUI` 只在初始化阶段显式调用。没有把业务逻辑伪装成空壳转发，也没有把 Tk 依赖塞入 Controller。
- Checkpoint 6 边界反向验证：临时向 `gui_style_setup.py` 引入 `gui_main` 后，新边界测试准确单点失败；违规导入已撤销并恢复全绿。
- Checkpoint 6 最终指标：`gui_main.py` 15,479 行，`BossFilterGUI` 508 方法；较基线减少 1,993 行和 18 方法，仍分别超过硬目标 4,979 行和 158 方法。
- Checkpoint 6 最终验证：1737/1737 单元回归，导入烟测通过；七页真实 Tk 创建/切换、结果强制刷新、更新日志弹窗通过；控件树保持 35/146/102/59/54/31/84；页面切换基准无失败。
- 最终只读审计：核心业务模块零差异；未运行真实 BOSS 扫描、联系发送、打包或发布，也未 push、merge、rebase、force push 或删除文件。
- 最终裁决：7 个 checkpoint 已用完。剩余主体是 Tk 宿主页胶水、弹窗/工作台 UI 和仍被调用的兼容面；现有 Controller 禁止导入 Tk，而本任务又未授权新建更多 GUI mixin/page/workbench 模块或继续修改既有 `gui_*` 页面，因此不能在不违反模块边界和文件范围的前提下达到 10,500/350。
- 最终白名单复核更正：第 7 个 checkpoint `f7f7dcd` 修改了 `tests/unit/test_ui_messagebox.py`，该文件不在任务书允许修改清单内。此前“工作区只含白名单改动”的表述不成立；测试全绿不能替代文件范围合规。
- 当前不能自行补第 8 个 checkpoint（任务书规定最多 7 个），也不能未经授权 amend 第 7 个提交改写本地历史；准确处置写入 `BLOCKED.md`，等待管理者决定是否授权一次纠偏提交或本地 amend。
- 白名单纠偏授权：管理者已明确授权“amend 第 7 个 checkpoint，仅修正白名单越界并重跑完整验收”。`tests/unit/test_ui_messagebox.py` 已恢复为与 `master` 完全一致；消息框字体注册留回 `gui_main.py`，其余全局 Tk/ttk 样式继续由 `gui_style_setup.py` 管理。
- 纠偏边界反向验证：临时向 `gui_style_setup.py` 加入 `import gui_main` 后，指定边界测试 1 项准确失败；撤销后 1/1 通过。
- 纠偏完整验收：使用任务书指定 `pack_venv` 执行，1737/1737 单元回归、导入烟测、七页真实 Tk、结果强制刷新、更新日志弹窗和控件树 35/146/102/59/54/31/84 全部通过；页面切换基准无失败，`git diff --check` 通过。
- 纠偏终值：`gui_main.py` 15,497 行，`BossFilterGUI` 508 方法；较基线减少 1,975 行和 18 方法，仍分别超过硬目标 4,997 行和 158 方法。白名单越界已解除，硬指标/模块范围阻塞仍存在。

# GUI Host 拆分进度

> 当前任务：`codex/gui-host-modularization-04`；从 PR #60 Squash 提交 `b326e4b87e23` 建立，不 push、不发布，checkpoint 交付另行授权。

- 目标：通过组合式 `gui_app_shell` 和后续 GUI 工作流模块继续降低 `BossFilterGUI` 影响范围；禁止多继承 mixin、一行转发凑数和反向导入 `gui_main`。
- 基线：`gui_main.py` 15,497 行，`BossFilterGUI` 508 方法；20 个方法不少于 100 行（3,173 行），65 个方法不少于 60 行（6,614 行）。
- 基线验证：PR #60 `PR Checks` 成功；本地/GitHub/Gitee `master` 同为 `b326e4b87e23`；真实 Tk 七页、结果强制刷新、更新日志和控件树 35/146/102/59/54/31/84 通过。
- Checkpoint 1 范围：先拆页面身份、侧边栏、导航视觉和首次按需打开；首页仍是唯一启动时创建的业务页，页面业务刷新仍由宿主显式回调。
- Checkpoint 1：新增组合式 `gui_app_shell.AppShell`，迁入页面注册表、侧边栏、导航状态/角标、分段按需创建、页面显示隐藏和响应式宽度策略；首页、结果页、统计页直接调用 `host.app_shell`，不为导航和宽度策略新增 `BossFilterGUI` 转发方法。
- Checkpoint 1 结构：`AppShell` 构造器显式接收 UI 配置、字体与版本，`AppShellHost` 明列页面创建/显示和局部布局回调；新模块不导入 `gui_main`、业务、存储、浏览器或网络。侧边栏内部拆为 Logo、样式、单项导航和版本区，`AppShell` 没有不少于 100 行的方法。
- Checkpoint 1 指标：`gui_main.py` 15,015 行，`BossFilterGUI` 499 方法；较基线减少 482 行和 9 方法。不少于 100 行的方法由 20 个/3,173 行降至 19 个/2,995 行；不少于 60 行的方法由 65 个/6,614 行降至 63 个/6,341 行。
- Checkpoint 1 验证：1738/1738 单元回归、导入烟测、七页真实 Tk、结果强制刷新、更新日志弹窗、控件树 35/146/102/59/54/31/84 和页面切换基准通过；热切换均为 1-3 ms。首次完整回归发现测试夹具传入空 UI 配置导致 2 项失败，已只修正夹具为生产 `UI_CONFIG`，未改生产逻辑或放宽断言。
- Checkpoint 1 反向验证：临时向 `gui_app_shell.py` 引入 `storage` 后，新边界测试准确单点失败；撤销违规导入后恢复全绿。核心筛选、存储、浏览器、联系和扫描模块零差异，未运行真实 BOSS 操作、打包或发布。
- Checkpoint 2 审计裁决：候选人菜单/复核剩余长方法主要承担联系资格、人工复核和业务回调装配，继续迁入 Tk builder 会违反既有业务边界；本 checkpoint 改拆无业务依赖的跨平台滚动生命周期。
- Checkpoint 2：新增组合式 `gui_scroll_support.ScrollSupport`，迁入 Canvas 滚动容器、自动显隐滚动条、滚轮增量归一化、Spinbox 有界滚轮、递归控件绑定、当前页面 Canvas 路由和 macOS Cocoa 触控板 hook；配置、运行、学历和岗位复盘页面直接调用 `host.scroll_support`，`BossFilterGUI` 不保留同名转发方法。
- Checkpoint 2 指标：`gui_main.py` 14,629 行，`BossFilterGUI` 493 方法；较 checkpoint 1 再减少 386 行和 6 方法，较 Host 阶段基线累计减少 868 行和 15 方法。不少于 100 行的方法降至 18 个/2,840 行，不少于 60 行的方法降至 59 个/5,987 行。
- Checkpoint 2 验证：1739/1739 单元回归、导入烟测、七页真实 Tk、结果强制刷新、更新日志弹窗、控件树 35/146/102/59/54/31/84 和页面切换基准通过。首次完整回归只有 1 项源码断言仍检查迁移前内部闭包名，已修正为新模块真实名称，未改生产行为或放宽能力断言。
- Checkpoint 2 反向验证：临时向 `gui_scroll_support.py` 引入 `storage` 后，新边界测试准确单点失败；撤销后指定边界与 1739/1739 完整回归恢复全绿。当前 Windows 已验证普通滚轮/页面创建链；Cocoa hook 保持原实现语义并有静态边界覆盖，但本机不是 macOS，未做真实触控板实测。
- Checkpoint 3 范围裁决：只拆输入控件右键菜单和通用只读文本弹窗；Tooltip、inline 横幅与自绘开关拥有不同生命周期，暂不塞入同一工具类。候选人/岗位文本解释和业务动作继续留在宿主，只把标题、普通文本和显式回调传入新模块。
- Checkpoint 3：新增组合式 `gui_input_support.InputSupport`，迁入 Entry/Combobox/Text 的剪切、复制、粘贴、全选右键菜单和通用模态只读文本弹窗；配置、设置、运行、学历、候选人待办/体检/复核页面直接调用 `host.input_support`，`BossFilterGUI` 不保留同名转发方法。
- Checkpoint 3 指标：`gui_main.py` 14,470 行，`BossFilterGUI` 490 方法；较 checkpoint 2 再减少 159 行和 3 方法，较 Host 阶段基线累计减少 1,027 行和 18 方法。不少于 100 行的方法为 18 个/2,844 行，不少于 60 行的方法降至 58 个/5,914 行；新模块 184 行且没有不少于 100 行的方法。
- Checkpoint 3 验证：1740/1740 单元回归首轮即通过；导入烟测、七页真实 Tk、结果强制刷新、更新日志弹窗、控件树 35/146/102/59/54/31/84 和页面切换基准通过。另用真实 Tk 创建带滚动文本区、关闭按钮和额外动作按钮的通用文本弹窗并成功销毁。
- Checkpoint 3 反向验证：临时向 `gui_input_support.py` 引入 `storage` 后，新边界测试准确单点失败；撤销后指定边界与 1740/1740 完整回归恢复全绿。核心筛选、存储、浏览器、联系和扫描模块仍无本 checkpoint 差异。
- Checkpoint 4 范围裁决：只拆通用 Tooltip 窗口、模型/技能/必要条件提示槽和页面 inline 横幅生命周期；何时展示、展示什么业务字段继续由页面或宿主决定，不把候选人、岗位或联系业务塞进反馈组件。
- Checkpoint 4：新增组合式 `gui_feedback_support.FeedbackSupport`，统一提示窗口创建、屏幕工作区限位、延迟任务取消、各提示槽销毁和横幅显示/关闭；候选人待办/体检、岗位配置、学历、结果和设置页面直接调用 `host.feedback_support`，`BossFilterGUI` 不保留同名转发方法。组件构造时初始化兼容槽位，使懒加载页面尚未创建时也可安全调用；横幅会在创建自身前捕获既有页面子控件，空白页面首次显示不再把未布局的自身作为 `before` 目标。
- Checkpoint 4 指标：`gui_main.py` 14,324 行，`BossFilterGUI` 480 方法；较 checkpoint 3 再减少 146 行和 10 方法，较 Host 阶段基线累计减少 1,173 行和 28 方法。不少于 100 行的方法保持 18 个/2,845 行，不少于 60 行的方法保持 58 个/5,915 行；新模块 252 行且没有不少于 100 行的方法。
- Checkpoint 4 验证：1741/1741 单元回归、导入烟测、七页真实 Tk、结果强制刷新、更新日志弹窗、控件树 35/146/102/59/54/31/84 和页面切换基准通过；另用真实 Tk 验证通用、模型、页面局部 Tooltip 与 inline 横幅的创建和销毁。新模块及直接调用页面的 Ruff 检查、相关源码编译和 `git diff --check` 通过。
- Checkpoint 4 反向验证：临时向 `gui_feedback_support.py` 引入 `storage` 后，新边界测试准确单点失败；撤销违规导入后指定边界与 1741/1741 完整回归恢复全绿。核心筛选、存储、浏览器、联系和扫描模块仍无本 checkpoint 差异；未运行真实 BOSS 操作、打包或发布。
- Checkpoint 5 范围裁决：只拆页面标题、通用卡片、自绘开关、通用空态和启动状态图标五类无业务语义控件原语；结果页空态显隐、表格列宽、页面布局流程和所有业务回调继续留在页面或宿主。无生产调用的 `_start_breathing` 本 checkpoint 不搬家，留给最终引用审计单独裁决。
- Checkpoint 5：新增组合式 `gui_widget_support.WidgetSupport`，配置、运行、设置、首页、岗位复盘、学历、结果和统计页面直接调用 `host.widget_support`，`BossFilterGUI` 不保留同名控件工厂转发方法；状态图标以不可变 `StatusIconSet` 返回并由宿主保留兼容引用。自绘开关的超采样渲染拆为内部纯视觉方法，新模块最大方法 73 行。
- Checkpoint 5 指标：`gui_main.py` 14,101 行，`BossFilterGUI` 475 方法；较 checkpoint 4 再减少 223 行和 5 方法，较 Host 阶段基线累计减少 1,396 行和 33 方法。不少于 100 行的方法保持 18 个/2,847 行，不少于 60 行的方法降至 57 个/5,834 行；新模块 341 行且没有不少于 100 行的方法。
- Checkpoint 5 验证：1742/1742 单元回归首轮通过，反向验证撤销后的最终完整回归再次全绿；导入烟测、七页真实 Tk、结果强制刷新、更新日志弹窗、控件树 35/146/102/59/54/31/84 和页面切换基准通过。新模块及九个直接调用页面的 Ruff 检查、相关源码编译和 `git diff --check` 通过。
- Checkpoint 5 反向验证：临时向 `gui_widget_support.py` 引入 `storage` 后，新边界测试准确单点失败；撤销违规导入后指定边界恢复全绿。核心筛选、存储、浏览器、联系和扫描模块仍无本 checkpoint 差异；未运行真实 BOSS 操作、打包或发布。
- Checkpoint 6 范围裁决：只拆窗口高屏/最大化判断、页面局部高度、结果统计紧凑态和结果/统计/模型/学历表格列宽；筛选口径、候选人数据、页面刷新业务和浏览器流程全部留在原模块。用户指南截图脚本同步改走新组合对象，避免删除宿主方法后留下失效调用。
- Checkpoint 6：新增组合式 `gui_layout_support.LayoutSupport`，迁出 15 个响应式布局方法；`AppShell`、设置页和学历页直接调用 `host.layout_support`，`BossFilterGUI` 不保留布局转发方法。新模块 496 行、16 个方法，最大方法 87 行。
- Checkpoint 6 指标：`gui_main.py` 13,744 行，`BossFilterGUI` 460 方法；较 checkpoint 5 再减少 357 行和 15 方法，较 Host 阶段基线累计减少 1,753 行和 48 方法。
- Checkpoint 6 验证：1745/1745 单元回归通过；新增高窗口页面增高、普通窗口维持基线、结果统计图标收起/恢复和依赖边界意图测试。导入烟测、七页真实 Tk、结果强制刷新、更新日志弹窗、控件树 35/146/102/59/54/31/84、页面切换基准和 3840x2160 本地 GUI 验收矩阵全部通过；新模块及直接调用生产模块 Ruff、相关源码编译和 `git diff --check` 通过。
- Checkpoint 6 反向验证：临时向 `gui_layout_support.py` 引入 `storage` 后，新边界测试准确失败；撤销违规导入后指定边界恢复全绿。未运行真实 BOSS 扫描、联系发送、打包或发布。
- Checkpoint 7 范围裁决：对 214 个受版本控制的 Python 文件做 AST、精确字符串动态引用和 `BossFilterGUI` 调用图复核；只删除确认没有生产调用链的 8 个宿主兼容方法与 2 个模块级兼容函数，不继续拆仍可达的 GUI 胶水。
- Checkpoint 7 测试归属：结果视图筛选、维护时间、终态日志、候选人最近经历和岗位复盘文本测试改为直接验证各自 Controller/Presenter；删除只为两个已废弃提示门面服务的文案测试，并增加兼容门面不得回流的边界测试。边界测试在生产删除前准确红灯，删除后转绿。
- Checkpoint 7 指标：`gui_main.py` 13,656 行，`BossFilterGUI` 452 方法；较 checkpoint 6 再减少 88 行和 8 方法，较 Host 阶段基线累计减少 1,841 行和 56 方法。214/214 个 Python 文件均可解析；包含全部外部引用时 355 个根、452 个方法全部可达，零不可达方法，10 个已删除入口均不存在。
- Checkpoint 7 验证：1744/1744 单元回归通过（14.93 秒）；导入烟测、七页真实 Tk、结果强制刷新、更新日志弹窗、控件树 35/146/102/59/54/31/84、页面切换基准和 3840x2160 本地 GUI 验收矩阵 9 项全部通过；Ruff F401/F821、源码编译和 `git diff --check` 通过。
- Checkpoint 7 最终裁决：本阶段最终引用清理完成。现有 452 个方法均有可达路径，继续仅凭文件长度删除或搬运会进入高风险业务胶水重构，不属于本阶段有证据支持的低风险拆分。未运行真实 BOSS 扫描、联系发送、打包或发布，也未 push、merge、rebase 或 force push。
