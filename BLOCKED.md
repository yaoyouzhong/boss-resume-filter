# GUI 性能实验历史待裁决记录

> 以下内容记录 2026-07-27 已结束的性能实验及未达成指标，不代表当前发布流程被阻断。

## 2026-07-27：最终审计仅 `App.__init__` 比率未通过

所有失败性能实现均已回滚，最终仍按同一会话“1 冷 + 3 次取中位”协议收账：

- `App.__init__=217ms`，会话基线 `206ms`，终值/基线 `105.3%`，目标 `≤55%`，失败。
- 三重页 create 合计 `198ms`，会话基线 `592ms`，比率 `33.4%`，通过。
- 总账 `452ms`，会话基线 `893ms`，比率 `50.6%`，通过。
- 热切换 r1 `3/2/2/1/1ms`，均未比基线劣化 20%，通过。

三重页和总账在没有保留性能代码的情况下仅因运行状态变化即大幅下降，不能把缓存/系统抖动包装成实现收益。唯一未满足的硬完成条件是 `App.__init__≤55%`；该指标已经按任务 1 规则连续失败三轮，不能继续修改。

## 2026-07-27：任务 2 三个重页面目标三连败

同步剖析确认配置、运行、结果页的创建时间几乎全部来自 Tk/Tcl 为 142、105、56 个控件逐个创建和首次布局；在控件数、属性与行为不变的前提下没有可删除的业务计算。三轮修改-验证均已回滚：

1. 分片预建首次实现的完成回调缺少生命周期属性，四次均 `AttributeError` 后超时；原基准总账中位数仍为基线 98.7%，未达 80%，stats 热切换也劣化。
2. 修正生命周期后，三页完整预建中位数 `2789ms`；预建后 `show_config=575ms`，为配置页 create 基线的 215%，隐藏页首次映射仍同步布局。
3. 在创建期提前把配置详情容器登记到布局后，`show_config=13ms`，达到预建打开目标；但 `create_config=461ms`，比基线 `267ms` 劣化 72.7%，明确属于被禁止的成本搬移。

不采用删控件、空壳页、临时替代控件或在启动后强制映射隐藏页的方案。按“同一指标连败 3 次换下一项”规则，停止任务 2，继续任务 3 对拍、冒烟和回归。

## 2026-07-27：任务 1 启动初始化相对目标三连败

会话基线为 `App.__init__=206ms`，目标为不高于基线 55%，即 `113.3ms`。三轮均严格采用“1 次冷跑丢弃 + 连跑 3 次逐项取中位数”，并在失败后完整回滚：

1. 删除三次无数据依赖的 `update_idletasks`：`init=255ms`，比基线劣化 23.8%。
2. 叠加延后终态进度图标和非首页导航高亮图标：`init=153ms`，为基线 74.3%，未达 55%；热切换多项越过 +20% 红线。
3. 再延后联系清单角标数据读取：`init=318ms`，为基线 154.4%；热切换 r1 中位数为 `24/10/14/10/9ms`，全面劣化。

三轮代码均已回滚，当前不保留性能更差的实现。测量结果继续表现出明显的同会话系统抖动，但修订令规定中途验证仍按固定 1+3 协议，不能另行放宽。按“同一指标连败 3 次换下一项并写 `BLOCKED.md`”规则，停止任务 1，继续任务 2。

## 2026-07-27：任务 0 性能基线与任务书不一致（已裁决：测量口径问题）

按任务书要求，在新建分支 `codex/gui-perf` 后首先运行：

```text
python tests/manual/bench_page_switch.py
```

实际完整输出：

```text
App.__init__: 1116 ms
create_home: 127 ms
create_config: 957 ms
create_run: 540 ms
create_result: 495 ms
create_stats: 145 ms
show_home r0: 887 ms
show_config r0: 748 ms
show_run r0: 21 ms
show_result r0: 57 ms
show_stats r0: 25 ms
show_home r1: 12 ms
show_config r1: 12 ms
show_run r1: 21 ms
show_result r1: 5 ms
show_stats r1: 4 ms
```

与任务书管理者基线相比：

| 指标 | 任务书基线 | 本次实测 | 差异 |
|---|---:|---:|---:|
| `App.__init__` | 839 ms | 1116 ms | +277 ms（+33%） |
| `create_config_page` | 556 ms | 957 ms | +401 ms（+72%） |
| `create_run_page` | 349 ms | 540 ms | +191 ms（+55%） |
| `create_result_page` | 272 ms | 495 ms | +223 ms（+82%） |
| `create_home_page` | 30 ms | 127 ms | +97 ms（+323%） |
| `create_stats_page` | 41 ms | 145 ms | +104 ms（+254%） |

命令退出码为 0，但关键数字全部明显偏高，不能视为正常测量抖动。任务书明确规定“命令不存在或对不上就停，证据写 `BLOCKED.md` 最上面”，因此已停止任务 0；尚未运行 `tests/run_unit_tests.py`、`tests/test_import.py`，也未创建控件树基线、修改 GUI 代码或开始任何修改-验证循环。

裁决结果：原任务书绝对毫秒基线作废。后续统一采用同一会话内“1 次冷跑丢弃 + 连跑 3 次取中位数”，以终值与会话基线的比率判定。原停止处置合格，阻塞已解除。

# GUI Controller 拆分待裁决记录

> 当前任务：`codex/gui-controller-modularization-03`。

## 最终硬指标受当前模块边界和文件范围阻塞

- 实测终值：`gui_main.py` 15,479 行，`BossFilterGUI` 508 方法；硬目标仍差 4,979 行和 158 方法。
- 全仓引用审计只发现 20 个可安全删除的无调用方法，均已删除；剩余方法存在真实调用或承担 Tk 页面、弹窗、导航、生命周期和兼容职责。
- Controller 明确禁止导入 Tk；把剩余 UI 代码搬进 Controller 会破坏本任务刚建立并反向验证过的依赖边界。
- 达标需要新增 GUI mixin/page/workbench 模块，或授权继续修改既有 `gui_*` 页面模块并迁移相应调用方；这超出本任务文件范围。
- 7 个 checkpoint 上限已用完。当前实现和全部验证保持可交付，但 10,500/350 不能宣称完成。

## 第 7 个 checkpoint 存在一项白名单越界

- `f7f7dcd` 为配合 `gui_style_setup.py` 的样式迁移，修改了 `tests/unit/test_ui_messagebox.py`；任务书只允许追加 `test_gui_job_config.py`、`test_gui_module_boundaries.py`、`test_gui_browser_matrix.py`，因此该文件属于越界修改。
- 当前分支其余已提交文件均在白名单内；`gui_style_setup.py` 符合“业务 Controller 拆分后仍超行数才允许拆纯样式”的条件。
- 若恢复该测试文件，必须把消息框字体注册的可验证实现留回 `gui_main.py`，然后重跑完整验收；这需要修改第 7 个 checkpoint。
- 任务书禁止超过 7 个 checkpoint；`git commit --amend` 又会改写本地提交历史，未获得单独授权前不执行。需要管理者明确授权“允许一次纠偏提交”或“允许 amend 第 7 个 checkpoint”。

### 已解除

- 管理者已明确授权 amend 第 7 个 checkpoint。
- `tests/unit/test_ui_messagebox.py` 已恢复为与 `master` 完全一致，消息框字体注册回到白名单内的 `gui_main.py`；指定 `pack_venv` 完整验收全绿。
- 本项白名单阻塞解除；上方 10,500 行/350 方法硬指标与当前文件范围冲突仍未解除。

## GUI Host 下一阶段授权

- 管理者已授权创建 `codex/gui-host-modularization-04` 并继续拆分；允许新增 `gui_app_shell.py` 及后续 GUI 工作流模块、修改相应 `gui_*` 调用方，上一阶段“文件范围不足”阻塞解除。
- 当前无新的外部阻塞；真实 BOSS 扫描、联系发送、正式发布仍不在本阶段范围内。
